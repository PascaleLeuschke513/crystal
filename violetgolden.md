# HyperLink Navigator

HyperLink Navigator 是一个面向技术社区与终端用户的轻量级外链资源聚合与导航系统。本项目定位于解决信息分散、优质资源入口难寻的问题，通过人工筛选与自动化检测相结合的方式，为开发者、研究者和兴趣爱好者提供高可用性的垂直领域 URL 资产目录。目标用户包括需要快速定位特定流媒体资源、实时体育数据接口或网络内容聚合服务的运维人员、前端工程师及数据分析师。

## 功能概览

- **智能链接状态监测**：系统每日定时检测收录资源的 HTTP 状态码与 DNS 解析情况，自动标记异常链接并生成可用性报告。
- **分类标签管理体系**：支持对每一枚收录 URL 添加多级自定义标签（如“直播”、“数据”、“韩国”），便于按业务场景快速过滤。
- **一键复制与短链生成**：为每个资源条目提供原始地址复制按钮及项目内短链映射服务，降低手动输入错误率。
- **自定义订阅列表**：用户可创建个人关注集合，当订阅资源发生状态变更或内容更新时，系统发送邮件通知。
- **访问统计看板**：记录每一条外链的点击频次、来源地域与时段分布，以图表形式呈现在管理后端。
- **黑暗模式与阅读优化**：前端界面适配亮色/深色主题，并针对长列表提供首字母索引与模糊搜索框。
- **开放 API 接口**：提供 RESTful API 用于批量导入/导出链接数据，支持第三方工具集成。

## 应用场景

- **运维监控人员**：每日通过 HyperLink Navigator 的仪表盘快速核查一批关键业务域名的可访问性，在故障前发现 DNS 或证书异常，替代手动 curl 脚本。
- **前端开发工程师**：在开发需要嵌入第三方直播或数据服务的模块时，利用本项目的分类导航迅速找到可用测试地址，并通过短链功能减少跨域配置麻烦。
- **数据采集研究员**：针对特定主题（如足球实时比分）批量收集相关 URL，利用项目提供的标签过滤与导出功能，快速构建初始采集种子列表。
- **技术内容运营者**：将本项目作为团队内部书签管理工具，通过订阅列表共享稳定资源变动信息，减少群聊中反复传递链接的沟通成本。

## 快速开始

以下步骤将帮助您在本地环境快速启动 HyperLink Navigator 实例。

```bash
# 1. 克隆仓库
git clone https://github.com/hyperlink-navigator/hln-core.git
cd hln-core

# 2. 安装依赖（使用 npm）
npm install

# 3. 启动开发服务器（默认端口 3000）
npm run dev
```

若需要使用生产环境构建，请执行 `npm run build` 并结合 `pm2` 或 `systemd` 进行部署。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | >= 18.0.0 | 核心运行时环境，推荐使用 LTS 版本 |
| npm | >= 9.0.0 | 包管理器，用于安装项目依赖 |
| SQLite3 | 系统级或嵌入式 | 默认轻量级数据库，用于存储链接元数据与状态 |
| Redis | >= 6.0 （可选） | 高级缓存与任务队列支持，提升大并发下的检测效率 |
| Nginx | >= 1.18 （反向代理推荐） | 用于生产环境代理静态资源与负载均衡 |
| 系统内存 | >= 512 MB | 最低运行内存要求，建议 1GB 以上以获得良好性能 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户指南 | [/docs/user-guide.md](docs/user-guide.md) | 如何注册、添加链接、创建订阅及使用看板？ |
| 管理员手册 | [/docs/admin-manual.md](docs/admin-manual.md) | 如何配置检测间隔、管理标签体系及审查异常链接？ |
| API 参考 | [/docs/api-reference.md](docs/api-reference.md) | 如何通过 REST API 批量操作资源数据或集成到自己的工具？ |
| 部署与运维 | [/docs/deployment.md](docs/deployment.md) | 如何在不同操作系统上部署生产版本，并配置 HTTPS 与反向代理？ |

## 资源列表

本项目的初始核心资源收录自公开互联网，按内容主题划分如下。所有链接均保留原始格式，不加协议修饰或域名变更，以便于您精准引用。

**综合直播与娱乐类**

*   <code>https://hanguomeinvzhuborewuw.org.cn</code>
*   <code>https://zaixianbofangzhubow.org.cn</code>
*   <code>https://zhubozhibozaixianguankanw.org.cn</code>

**体育实时数据类**

*   <code>https://zuqiujishibifend.org.cn</code>
*   <code>https://zuqiujishibifene.org.cn</code>
*   <code>https://zuqiujishibifenf.org.cn</code>
*   <code>https://zuqiujishibifeng.org.cn</code>

## 项目结构

项目采用功能模块化分层设计，主要目录及功能说明如下：

```
hln-core/
├── src/                           # 核心源代码目录
│   ├── api/                       # RESTful API 路由与控制器
│   ├── core/                      # 链接检测、短链生成、统计核心逻辑
│   ├── models/                    # 数据模型定义（链接、用户、标签等）
│   ├── services/                  # 与外部服务交互层（Redis、邮件）
│   └── utils/                     # 通用工具函数（日志、格式化、验证）
├── frontend/                      # 前端界面源码
│   ├── components/                # Vue/React 可复用 UI 组件
│   ├── pages/                     # 各视图页面（首页、看板、管理）
│   └── assets/                    # 样式、图片、静态资源
├── docs/                          # 完整项目文档（含 API 与部署手册）
├── tests/                         # 单元测试与集成测试脚本
│   ├── unit/                      # 核心模块单元测试
│   └── e2e/                       # 端到端浏览器自动化测试
├── scripts/                       # 运维与辅助脚本（数据迁移、检测任务）
├── config/                        # 环境配置文件（开发、测试、生产）
├── logs/                          # 运行时日志存储目录（自动生成）
├── package.json                   # 项目依赖清单与脚本入口
└── README.md                      # 您当前正在阅读的项目概览文档
```

## 贡献指南

欢迎并感谢您对本项目的关注与贡献。请遵循以下流程参与开发：

1.  **查阅问题列表**：访问 GitHub Issues 板块，查找标记为 `help wanted` 或 `good first issue` 的任务，避免工作冲突。
2.  **派生仓库并创建分支**：将主仓库 Fork 至您的个人账号，然后基于 `main` 分支创建以 `feature/` 或 `fix/` 为前缀的命名分支。
3.  **编写与自测**：在本地完成代码编写，并执行 `npm run test` 确保所有现有测试用例通过。为新功能补充相应的单元测试。
4.  **提交变更记录**：提交信息（commit message）需遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范，清晰描述变更类型与内容。
5.  **发起合并请求**：将您的分支推送至 GitHub，并向主仓库的 `main` 分支提交 Pull Request。请填写 PR 模板中的检查清单，并关联相关 Issue 编号。

## 常见问题

**问：系统检测到某个链接返回 403 状态码，这意味着什么？**

答：403 状态通常表示目标服务器理解了您的请求，但基于权限控制（如 IP 地理位置限制、缺少特定的请求头或验证凭证）而拒绝访问。本项目仅做可达性检测，不会伪造身份绕过服务器策略。若您确认该资源本应公开，可尝试通过项目前端的“手动验证”按钮触发一次即时检测，或检查该 URL 是否在目标站点的反爬白名单之外。对于 <code>https://hanguomeinvzhuborewuw.org.cn</code> 这类链接，若持续返回 403，建议联系其站点管理员确认访问策略，或更换网络环境重试。

**问：如何将本项目已收录的链接导出为 JSON 或 CSV 格式？**

答：您可以通过项目开放的 API 接口进行批量导出。调用 `GET /api/v1/links/export?format=json` 即可获取完整链接列表（含标签、状态、最后检测时间）。若需要 CSV，请将 format 参数改为 `csv`。此功能需要管理员权限，请使用管理员账户的 Bearer Token 进行身份认证。

**问：短链服务生成的别名有效期是多久？**

答：默认情况下，项目生成的短链永久有效，除非该原始链接被管理员手动移除或标记为不可用。系统不会自动清理短链映射记录。若需要为短链设置过期时间，可在管理后台的“短链配置”中调整全局 TTL（生存时间）参数。

## 许可证

本项目采用 MIT 许可证进行分发。您可以在遵守许可证条款的前提下自由使用、修改、复制及商用本项目代码。详细内容请参阅项目根目录下的 [LICENSE](LICENSE) 文件。

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:33
