# HyperLink Nexus

HyperLink Nexus 是一个面向技术内容创作者、开源项目维护者及数字档案管理者的高可用外链资源聚合与导航系统。本项目并非传统的爬虫或链接检查工具，而是一个基于静态站点生成逻辑的链接治理平台，旨在解决个人与团队在维护大量外部引用 URL 时面临的链接失效、域名劫持、内容溯源困难等核心痛点。目标用户包括技术文档撰写者、开源社区运营人员、学术研究辅助人员以及需要长期保存网络引用快照的合规性部门。

系统通过预设的规则引擎对导入的链接列表进行自动化分类、状态快照记录及存活度预检，并生成结构化的导航目录。项目本身不存储任何第三方内容，仅提供基于元数据的索引与跳转服务，确保轻量级部署的同时，最大程度提升外链管理的透明度与可维护性。

## 功能概览

- **批量链接注入与解析**：支持通过 JSON 或纯文本行批量导入 URL 列表，系统自动识别协议头、域名层级及路径参数，并提取有效根域用于后续分类。
- **状态快照与变更检测**：在用户触发检查时，通过异步任务对每个链接执行 HTTP HEAD/GET 请求，记录响应状态码、内容长度及最后修改时间，生成当前批次的存活状态报告。
- **分类标签系统**：基于域名特征与路径关键词，内置多套分类规则（如视频站点、文档库、社区论坛、学术资源），允许用户自定义标签，实现链接的多维度分组。
- **只读导航页面生成**：根据分类结果，自动生成纯静态的 HTML 目录页，支持按标签、域名前缀或导入批次进行筛选与排序，便于嵌入其他文档系统。
- **外部引用溯源**：记录每个链接的导入来源（如具体项目文档或会议记录编号），支持反向查询某资源被哪些内部页面引用，辅助进行依赖影响分析。
- **自动协议兼容处理**：在导航生成阶段，系统会识别裸域名并自动补充当前页面的协议头，同时保留用户原始输入的协议字符串用于展示，确保跳转行为符合用户预期。
- **变更日志审计**：所有链接的添加、删除及状态变更操作均记录至本地 SQLite 数据库，支持按时间轴回溯，满足内部审计需求。

## 应用场景

1.  **开源项目文档站外链接维护**：开源项目 README 或 Wiki 中常包含大量指向外部工具、论文或视频教程的链接。维护者可使用 HyperLink Nexus 定期检查这些链接的有效性，避免用户访问失效资源，提升项目文档的专业度。
2.  **技术博客聚合与分类导航**：技术博主或内容平台编辑可将分散在不同文章中的外部引用统一导入系统，自动生成按主题分类的导航页，方便读者快速定位相关扩展阅读材料。
3.  **学术参考文献网络存档**：研究人员在撰写综述或技术报告时，可将引用的所有在线资源链接纳入系统管理，系统生成的快照报告可作为附录材料，证明引用内容在特定时间点的可访问性。
4.  **企业内部知识库外链治理**：大型企业的内部技术文档库中，外链数量庞大且来源复杂。运维团队可定期运行本系统扫描所有文档页面中的链接，及时发现并替换已变更的域名或失效地址。
5.  **合规性审查前置检查**：在对外发布包含外部链接的正式文档前，法务或合规人员可利用本系统快速生成链接清单及状态报告，确保无指向违规或不可控站点的引用。

## 快速开始

以下步骤将指导您在本地环境完成项目的克隆、依赖安装与服务启动。

```bash
# 1. 克隆代码仓库
git clone https://github.com/nexus-user/hyperlink-nexus.git
cd hyperlink-nexus

# 2. 安装项目依赖（使用 pip 与虚拟环境）
python3 -m venv venv
source venv/bin/activate  # Windows 请使用 venv\Scripts\activate
pip install -r requirements.txt

# 3. 初始化本地数据库与配置
python manage.py init --db
python manage.py migrate

# 4. 导入示例链接数据（或替换为您的链接文件）
python manage.py import --file samples/links_batch_69.json

# 5. 启动开发服务
python manage.py runserver --host 0.0.0.0 --port 8080
```

## 安装要求

在部署 HyperLink Nexus 前，请确保您的环境满足以下依赖要求。所有组件均为开源或具有广泛使用的商业版本，可运行于主流 Linux 发行版、macOS 及 Windows WSL 环境。

| 依赖组件 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Python | 3.9 及以上 | 核心运行环境，用于启动调度器与 API 服务 |
| SQLite | 3.35 及以上 | 内置轻量级数据库，用于存储链接元数据与审计日志 |
| pip | 21.0 及以上 | Python 包管理工具，用于安装 requirements.txt 中列出的依赖库 |
| Git | 2.25 及以上 | 用于克隆仓库及后续版本更新，非运行时必需但建议安装 |
| 系统时区数据 | 任意 | 用于准确记录状态检查的时间戳，Linux 下通常由 tzdata 包提供 |
| 网络访问能力 | 出站 80/443 | 执行链接存活检测时需要访问外部 HTTP/HTTPS 服务，需开放相应出站端口 |
| 磁盘空间 | 至少 100 MB | 用于存储数据库文件、日志及生成的静态页面，实际需求取决于链接数量与快照频次 |
| 进程管理工具（可选） | 任意 | 生产环境建议搭配 systemd 或 supervisor 以实现进程守护 |

## 文档导航

为帮助不同角色的用户快速找到所需信息，项目文档按使用维度划分如下层级。新用户建议从“用户手册”开始，运维人员可重点阅读“部署与调优”章节。

| 层面 | 目录/章节 | 回答的问题 |
| :--- | :--- | :--- |
| 用户手册 | 快速开始 / 链接管理 | 如何导入第一批链接？如何查看当前所有链接的存活状态？ |
| 用户手册 | 导航生成 / 分类配置 | 如何自定义链接分类标签？生成的导航页面在哪里访问？ |
| 运维指南 | 安装要求 / 部署与调优 | 生产环境需要哪些额外依赖？如何调整异步检查的并发数？ |
| 运维指南 | 数据库备份 / 迁移 | SQLite 数据库文件如何备份？升级版本时是否需要执行迁移脚本？ |
| 开发者指南 | API 参考 / 插件系统 | 是否支持通过 REST API 进行链接管理？如何扩展自定义分类规则？ |
| 开发者指南 | 项目结构 / 贡献指南 | 核心代码分布在哪些模块？如何提交新的功能或修复补丁？ |
| 常见问题 | 故障排除 / 已知限制 | 遇到检查超时或解析错误时如何处理？系统支持的最大链接数量是多少？ |

## 资源列表

本章节收录本项目当前批次（第 69/130 批）所管理的全部外部资源链接。所有链接均保持用户提供的原始格式，未做任何协议补全或域名规范化处理，用于验证系统的分类与解析能力。

**视频与直播类资源**

- <code>https://rihanmeinvzhongwenzimub.org.cn</code>
- <code>https://qingqingcaoyuanzhongwenzimub.org.cn</code>
- <code>https://wanghongzhibozaixianshipin.org.cn</code>
- <code>https://wanghongfulizhibo.org.cn</code>
- <code>https://guochanwanghongzhibozhuzaixian.org.cn</code>
- <code>https://guochanwanghongshipinzhibo.org.cn</code>
- <code>https://wanghongzhibomianfeiguankan.org.cn</code>

## 项目结构

项目采用模块化分层设计，核心逻辑与配置、数据、静态资源分离，便于维护与扩展。以下为项目根目录的 ASCII 树状结构示意，每行附带简要功能注释。

```
hyperlink-nexus/
├── manage.py                 # 项目统一命令行入口，集成导入、检查、生成等子命令
├── requirements.txt          # Python 依赖清单，包含 Flask、requests、APScheduler 等
├── config/
│   ├── __init__.py           # 配置模块初始化
│   ├── settings.py           # 全局配置项（数据库路径、检查超时、并发数等）
│   └── rules.yaml            # 用户可编辑的分类规则定义文件
├── core/                     # 核心业务逻辑模块
│   ├── __init__.py
│   ├── linker.py             # 链接解析、规范化与元数据提取
│   ├── checker.py            # 异步存活状态检查与结果缓存
│   ├── classifier.py         # 基于规则与标签的分类引擎
│   └── generator.py          # 静态导航页面的生成器
├── storage/                  # 数据持久化层
│   ├── __init__.py
│   ├── database.py           # SQLite 连接池与基础 CRUD 操作
│   ├── models.py             # 数据表映射（链接表、批次表、日志表）
│   └── migrations/           # 数据库迁移脚本存放目录
├── web/                      # Web 服务与视图层（可选界面）
│   ├── __init__.py
│   ├── app.py                # Flask 应用工厂
│   ├── routes/               # API 与页面路由
│   └── templates/            # Jinja2 模板文件，用于输出导航页
├── samples/                  # 示例数据与测试用例
│   ├── links_batch_69.json   # 当前批次原始数据样例
│   └── test_cases.yaml       # 单元测试分类边界用例
├── scripts/                  # 运维辅助脚本
│   ├── backup_db.sh          # 数据库定时备份脚本
│   └── clean_cache.py        # 清理过期检查结果
├── logs/                     # 运行时日志存储目录（gitignored）
└── docs/                     # 详细文档源码，用于生成用户手册
    ├── user_guide.md
    └── dev_guide.md
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，包括但不限于功能建议、Bug 报告、代码提交及文档完善。为确保协作顺畅，请遵循以下流程：

1.  **议题讨论**：在提交 Pull Request 前，请先在 GitHub Issues 中搜索是否已有相关议题。若无，则新建一个议题详细描述您希望解决的问题或新增的功能，并等待维护者反馈。
2.  **分支开发**：从主分支 `main` 派生新的功能分支，分支命名建议遵循 `feature/简述` 或 `fix/简述` 格式。所有开发应基于最新的主分支代码。
3.  **代码与测试**：提交的代码需通过现有单元测试，并为新增功能编写对应的测试用例。请确保代码风格符合项目配置的 flake8 规则，并添加必要的 docstring。
4.  **提交信息规范**：提交信息应清晰描述变更内容，推荐使用 `<类型>: <主题>` 格式，类型包括 `feat`、`fix`、`docs`、`refactor` 等。
5.  **发起 Pull Request**：将您的分支推送到远程仓库后，在主仓库发起 Pull Request，并关联相关议题编号。PR 描述中请列出测试结果与变更摘要。维护者将在 3 个工作日内进行 Review。

## 常见问题

**Q1: 系统能否处理非标准端口或自签名证书的链接？**

A1: 可以。在 `config/settings.py` 中，您可以分别设置 `CHECK_ALLOW_INSECURE = True` 以允许自签名证书，并通过 `CHECK_PORT_WHITELIST` 配置允许检测的非标准端口列表。需要注意的是，对于非标准端口的检测，系统会依赖 `requests` 库的完整 URL 解析能力，只要提供的链接包含正确的端口号即可。

**Q2: 导入数千条链接时，系统性能表现如何？**

A2: 系统设计之初即考虑到批量处理场景。异步检查器默认采用协程池（基于 gevent）并发执行状态检测，对于 5000 条以下的链接，全量检查通常在 5 分钟内完成（取决于网络质量）。数据库层面针对 `url_hash` 和 `batch_id` 建立了复合索引，导入与查询操作均可在毫秒级响应。若链接数量超过 10000，建议分批导入并调整 `CHECKER_CONCURRENCY` 参数。

**Q3: 生成的静态导航页能否自定义样式？**

A3: 可以。导航页的生成逻辑位于 `core/generator.py`，模板文件位于 `web/templates/` 目录下。您可以直接修改 `navigation_template.html` 中的 HTML 结构与 CSS 样式，或通过 `config/settings.py` 中的 `NAV_THEME` 参数选择内置的几套配色方案。修改后重新运行 `generate` 命令即可应用新样式，无需重启整个服务。

## 许可证

本项目采用 MIT 许可证进行开源。您可以自由使用、修改、复制、分发本软件，包括将本软件用于商业目的。完整的许可证文本请参见项目根目录下的 LICENSE 文件。MIT 许可证仅覆盖本项目的源代码及文档，不涉及通过本系统索引或跳转的任何外部第三方资源，用户在使用本系统时应自行确保对外部链接的引用符合相关法律法规及内容提供者的使用条款。

> 外链数量: 7 | 生成时间: 2026-08-14 22:07:51
