# CloudView 技术资源聚合导航

CloudView 是一个面向开发人员、技术研究者与内容消费者的开源轻量级导航与资源聚合平台。该项目定位为技术化外链管理与信息筛选工具，帮助用户从繁杂的网络资源中快速定位高价值影视、字幕、多媒体技术测试样本及流媒体性能测试源。

项目目标用户包括：需要测试流媒体播放器兼容性的前端工程师、研究网络传输协议的视频技术爱好者、进行音画质量主观评价的品质保障人员，以及希望构建个人媒体库的资源整理者。通过结构化分类与稳定的外链聚合，CloudView 提供一站式的技术测试素材与参考资源索引，有效降低资源发现成本，提升技术验证效率。

## 功能概览

- **智能资源分类索引**：按内容来源、语种、编码格式等维度对聚合链接进行自动化标签归类，支持快速筛选与定位。

- **外链健康状态检测**：内置定期链接可用性巡检机制，对返回非200状态码的资源进行自动标记与告警，确保索引库的鲜活度。

- **自定义分类视图**：允许用户基于项目、团队或个人偏好创建专属资源分组，实现个性化导航布局。

- **轻量级全文检索**：支持对资源标题、描述、标签及域名进行快速关键词匹配检索，响应时间低于200毫秒。

- **RESTful 数据API**：提供标准的JSON格式数据输出接口，便于第三方工具集成与自动化工作流构建。

- **容器化一键部署**：提供官方Docker镜像与Kubernetes Helm Chart，支持在主流云平台或本地环境快速部署。

- **访问统计与热度分析**：记录资源点击频次与趋势，生成简洁的访问热力图，辅助用户识别高频资源。

- **暗色主题与阅读模式**：内置深色/浅色主题切换，并提供专注阅读模式，减少视觉干扰。

## 应用场景

1. **播放器兼容性测试**：前端开发者在适配不同浏览器或播放器内核时，使用本平台聚合的多种格式、多种码率的视频样本链接进行快速回归测试，验证解码与渲染表现。

2. **网络传输性能评估**：网络工程师利用平台中不同地理分布、不同运营商线路的资源链接，进行CDN加速效果、带宽占用及缓冲策略的对比分析。

3. **内容安全与风控研究**：安全研究人员通过平台收集的各类域名及资源，分析域名特征、页面重定向行为及内容加载策略，用于开发或验证反钓鱼、反欺诈规则模型。

4. **个人技术笔记与资源收藏**：技术博主或知识工作者将常用技术参考视频、字幕素材、测试页面通过本平台进行统一书签式管理，并按项目维度组织，便于分享与协作。

5. **自动化监控脚本数据源**：运维人员利用平台提供的稳定API接口，将资源链接作为拨测任务的目标URL，集成至现有监控系统，实现服务可用性主动探测。

## 快速开始

以下步骤指导您在本地开发环境快速启动 CloudView 服务。

```bash
# 1. 克隆项目仓库
git clone https://github.com/cloudview-io/cloudview-navigator.git
cd cloudview-navigator

# 2. 安装项目依赖（使用 pnpm）
pnpm install

# 3. 复制环境变量示例文件并配置
cp .env.example .env.local

# 4. 初始化本地数据库（SQLite）
pnpm run db:init

# 5. 启动开发服务器（默认端口 5173）
pnpm run dev
```

启动成功后，访问 `http://localhost:5173` 即可体验本地运行版本。生产环境部署请参考 `docs/deployment.md` 文档。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | >= 18.17.0 | 推荐使用 LTS 版本，用于运行服务端与构建工具 |
| pnpm | >= 8.0.0 | 高性能包管理器，用于依赖安装与工作区管理 |
| SQLite | >= 3.39.0 | 默认嵌入式数据库，适合开发与小型生产场景 |
| PostgreSQL | >= 14.0 (可选) | 生产环境推荐使用，需额外配置连接字符串 |
| Redis | >= 7.0 (可选) | 用于会话存储与缓存加速，提升高并发响应能力 |
| Docker | >= 20.10.0 (可选) | 容器化运行环境，用于一键部署与隔离测试 |
| Git | >= 2.30.0 | 用于克隆仓库及版本控制操作 |

## 文档导航

| 层面 | 目录/文档 | 回答的问题 |
| :--- | :--- | :--- |
| 用户指南 | `/docs/user-guide/` | 如何使用分类检索、自定义视图、快捷键与主题切换？ |
| 开发者文档 | `/docs/developer/` | 如何二次开发、扩展资源解析器或贡献新功能？ |
| 部署运维 | `/docs/operations/` | 如何配置生产环境变量、启用HTTPS、进行数据备份？ |
| API 参考 | `/docs/api/` | 资源索引、健康状态、统计数据的接口规范与调用示例？ |
| 设计理念 | `/docs/design/` | 项目架构决策、数据模型设计及技术选型理由？ |

## 资源列表

本平台聚合的外部资源链接均来源于公开网络，用于技术测试与性能验证。所有链接按类别组织如下：

### 国产影视内容测试源

<code>https://guochanjingpinzaixianmianfeikan.org.cn</code>

### 字幕辅助测试源

<code>https://zhongwenzimuzaixianyingshiyuan.org.cn</code>

### 免费高清流媒体测试源

<code>https://mianfeiguankanzaixianguankan.org.cn</code>

### 怀旧影视资源测试源

<code>https://jiujiushipinzaixianguankan.org.cn</code>

### 欧美影视内容测试源

<code>https://oumeizaixianguankanshipin.org.cn</code>

### 日韩影视内容测试源

<code>https://rihanshipinmianfeizaixianguankan.org.cn</code>

### 综合高清视频测试源

<code>https://mianfeigaoqingshipinzaixianguankan.org.cn</code>

## 项目结构

项目采用 Monorepo 方式组织，核心代码位于 `packages/` 目录下，结构清晰，职责分明。

```
.
├── apps/
│   ├── web/                         # 主前端应用 (Vue 3 + Vite)
│   │   ├── src/
│   │   │   ├── views/               # 页面视图组件
│   │   │   ├── components/          # 可复用UI组件库
│   │   │   ├── composables/         # 组合式逻辑 (状态管理、数据获取)
│   │   │   ├── router/              # 路由配置与守卫
│   │   │   └── styles/              # 全局主题与变量
│   │   └── index.html               # 入口HTML
│   └── server/                      # 后端API服务 (Fastify + Prisma)
│       ├── src/
│       │   ├── routes/              # 路由处理器 (资源、分类、健康)
│       │   ├── services/            # 业务逻辑层 (爬虫、检测、统计)
│       │   ├── models/              # 数据模型定义 (Prisma Schema)
│       │   ├── middleware/          # 认证、日志、跨域中间件
│       │   └── utils/               # 通用工具函数 (URL解析、日期)
│       └── prisma/                  # 数据库迁移与种子数据
├── packages/
│   ├── shared/                      # 前后端共享类型定义与常量
│   └── crawler/                     # 独立资源健康检查微服务
├── docker/
│   ├── Dockerfile.web               # 前端容器构建文件
│   ├── Dockerfile.server            # 后端容器构建文件
│   └── docker-compose.yml           # 本地开发组合编排
├── docs/                            # 完整项目文档 (用户、开发、部署)
├── scripts/                         # 辅助脚本 (数据初始化、迁移)
├── .env.example                     # 环境变量配置模板
└── README.md                        # 项目概览与快速入门 (本文件)
```

## 贡献指南

我们欢迎社区贡献，包括但不限于新增资源分类规则、优化前端性能、修复后端检测逻辑及完善文档。请遵循以下步骤：

1. **查阅路线图**：访问 `/docs/roadmap.md` 了解当前开发计划与优先级，避免重复工作。重大变更前建议先通过 Issue 讨论。

2. **派生与克隆**：将项目 Fork 至个人账号，并克隆到本地开发环境。确保使用 `main` 分支作为基线。

3. **创建特性分支**：基于 `main` 分支创建以 `feat/` 或 `fix/` 为前缀的分支名称，例如 `feat/add-rtmp-resource-type`。

4. **编码与测试**：遵循项目 ESLint 与 Prettier 配置。为新增的后端服务或前端组件编写对应的单元测试 (Vitest) 或 e2e 测试 (Playwright)。

5. **提交变更**：提交信息遵循 Conventional Commits 规范，例如 `feat(server): add link health check retry strategy`。提交前需通过所有 CI 检查。

## 常见问题

**问：启动开发服务器时，提示 SQLite 数据库连接失败，应如何解决？**

答：请先确认系统已安装 SQLite 3.39 或更高版本。若已安装，请检查项目根目录下是否有 `data/` 文件夹及 `dev.db` 文件。若无，请执行 `pnpm run db:init` 命令初始化数据库结构并生成种子数据。若依然失败，可尝试删除 `data/dev.db` 文件后重新初始化。

**问：平台中聚合的资源链接返回 403 或 404 状态，如何维护？**

答：平台内置的健康检查服务会每隔 24 小时自动巡检所有链接状态。状态异常的资源会在管理后台标记为“失效”，并在前端界面进行视觉弱化提示。项目维护者或贡献者可定期人工复核这些链接，根据实际情况更新为有效地址或从索引中移除。用户也可通过页面上的“报告失效”按钮主动提交反馈。

**问：是否支持导入或导出个人收藏的资源列表？**

答：目前版本支持以 JSON 格式导出当前用户创建的所有自定义分组及其中收藏的链接。您可以在“个人设置” - “数据管理”中找到导出功能。导入功能正在开发中，预计在 v1.3.0 版本上线。

## 许可证

本项目采用 MIT 许可证。您可以在遵守许可证条款的前提下自由使用、修改、分发本项目代码，包括用于商业目的。完整的许可证文本请参阅项目根目录下的 `LICENSE` 文件。

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:27
