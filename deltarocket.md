# TechLink Navigator

TechLink Navigator 是一个面向开发者与技术决策者的外链资源聚合与管理平台，定位于高质量技术信息节点的结构化整理与快速导航。项目不生产内容，而是通过人工筛选与社区投票机制，将分散在各类技术社区、官方文档、自建博客中的优质外链进行语义归类、可用性检测与版本追踪，解决开发者在信息检索过程中面临的链接失效、质量参差、上下文缺失三大痛点。

目标用户包括：需要频繁查阅技术规范与 API 文档的研发工程师、负责技术选型与架构评审的团队 leader、以及撰写技术文章或教程的内容创作者。通过 TechLink Navigator，用户可在统一面板内完成从关键词检索到目标页面跳转的全流程，无需反复在搜索引擎与收藏夹之间切换。

## 功能概览

- **多维度标签分类**：每条外链支持最多 5 个自定义标签，涵盖编程语言、框架版本、应用场景、维护状态、语言区域，便于按任意组合过滤。
- **链接可用性实时检测**：系统每日自动检测已收录链接的 HTTP 状态码与响应时长，标记失效或降速节点，并提供最近三次检测历史。
- **社区投票与质量评分**：注册用户可对链接内容进行专业度、清晰度、时效性三方面打分，综合评分影响搜索结果排序权重。
- **快照与上下文备注**：支持为每个外链添加不超过 500 字的上下文说明，记录该链接的核心价值、适用版本或注意事项，快照功能保存链接首次收录时的页面摘要。
- **批量导入与导出**：支持标准 CSV 与 JSON 格式的链接批量导入，导出支持 Markdown 列表、HTML 书签文件及纯文本清单三种格式。
- **个人收藏集与共享**：用户可创建多个收藏集（如“Go 微服务脚手架”“Kubernetes 排障手册”），每个收藏集可生成独立只读分享链接。
- **RSS 订阅与更新通知**：对任意标签组合或收藏集生成专属 RSS 订阅源，当链接状态变更或新增关联链接时推送更新。
- **暗色模式与阅读视图**：前端界面提供暗色主题切换，外链跳转前可预览目标页面的纯文本阅读视图，减少广告与侧边栏干扰。

## 应用场景

- **技术调研期的信息归档**：架构师在进行消息中间件选型时，可将官方文档、性能测试报告、社区对比文章、生产环境案例统一收录至临时收藏集，通过备注字段记录各方案优劣，最终导出对比报告供团队评审。
- **新人入职培训路径构建**：团队负责人可将内部 Wiki、编码规范、CI/CD 流水线文档、核心业务模块设计稿、常用开发工具下载页整理为“新人起步”收藏集，生成分享链接发给新成员，替代零散的邮件与聊天记录转发。
- **技术博客写作素材管理**：内容创作者围绕“分布式事务”主题收集数十篇参考文章与开源项目地址，使用 TechLink Navigator 打上“Saga”“TCC”“AT”等标签，写作过程中按标签快速调取，避免频繁切换浏览器标签页。
- **离线环境下的链接可用性审计**：内部网络受限的企业用户可定期导出全部链接的可用性检测报告，识别哪些外网资源无法访问，以便提前准备离线镜像或内部代理。
- **开源项目 README 外链维护**：开源项目维护者可将项目依赖的规范文档、教程、相关工具链链接集中托管于 TechLink Navigator，通过 RSS 订阅获知链接失效，及时更新项目文档中的引用地址。

## 快速开始

以下命令可在本地环境完成 TechLink Navigator 服务端的克隆、依赖安装及开发运行。

```bash
# 克隆代码仓库
git clone https://github.com/techlink-navigator/navigator-core.git

# 进入项目目录
cd navigator-core

# 安装依赖（使用 npm，Node.js 18+）
npm install

# 复制环境变量模板并填写必要配置
cp .env.example .env

# 初始化数据库（SQLite 默认，生产环境可切换至 PostgreSQL）
npm run db:migrate

# 启动开发服务器（默认监听 3000 端口）
npm run dev
```

启动成功后，访问本地 3000 端口即可进入控制台界面。首次运行将自动创建管理员账户，初始密码输出于终端日志中，请及时修改。

## 安装要求

| 依赖 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.17.0 或更高 | 运行时环境，推荐使用 LTS 版本 |
| npm | 9.0.0 或更高 | 包管理器，用于安装项目依赖 |
| SQLite | 3.35.0 或更高（内置） | 开发环境默认数据库，无需额外安装 |
| PostgreSQL | 14.0 或更高（可选） | 生产环境推荐，需自行安装并配置连接字符串 |
| Redis | 7.0 或更高（可选） | 用于会话存储与缓存，非必需但可提升性能 |
| Git | 2.30.0 或更高 | 版本控制，用于克隆仓库与提交变更 |
| Docker | 24.0 或更高（可选） | 容器化部署方案，配合 docker-compose 使用 |
| 操作系统 | Linux / macOS / Windows（WSL2） | 跨平台支持，Windows 下建议使用 WSL2 以获得最佳性能 |
| 浏览器 | 基于 Chromium 或 Firefox 的现代版本 | 管理控制台前端要求 ES2020 特性支持 |
| 网络 | 外网访问能力（用于链接检测） | 可用性检测功能需要从服务端发起对外 HTTP 请求 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | /docs/user/quick-start.md | 如何注册账号、创建收藏集、导入导出链接、使用 RSS 订阅 |
| 管理员指南 | /docs/admin/deployment.md | 生产环境如何部署、配置反向代理、设置定时任务、备份数据库 |
| 开发者文档 | /docs/developer/api-reference.md | API 鉴权方式、请求响应结构、限流策略、错误码定义 |
| 设计决策 | /docs/design/architecture.md | 系统模块划分、数据模型 ER 图、缓存策略、扩展性设计 |
| 运维手册 | /docs/ops/monitoring.md | 健康检查端点、日志级别配置、性能指标采集、告警规则 |
| 贡献规范 | /docs/contributing/coding-standards.md | 代码风格、提交信息格式、PR 流程、测试覆盖率要求 |
| 安全说明 | /docs/security/audit-log.md | 审计日志记录内容、敏感字段脱敏规则、CORS 配置建议 |
| 版本记录 | /docs/releases/changelog.md | 每个版本的更新摘要、破坏性变更、升级注意事项 |

## 资源列表

### 技术分析站点

<code>https://yijiajishibifenb.org.cn</code>

<code>https://fajiajishibifenb.org.cn</code>

### 体育赛事数据

<code>https://zuqiubisaijieguob.org.cn</code>

<code>https://yingchaobifenb.org.cn</code>

### 综合比分与统计

<code>https://xijiabifenb.org.cn</code>

<code>https://dejiabifenb.org.cn</code>

<code>https://yijiabifenb.org.cn</code>

## 项目结构

```
navigator-core/
├── src/
│   ├── api/                            # HTTP 路由与控制器层
│   │   ├── v1/                         # API 版本 v1 路由定义
│   │   │   ├── links/                  # 链接资源 CRUD 端点
│   │   │   ├── collections/            # 收藏集管理端点
│   │   │   ├── users/                  # 用户注册与个人资料端点
│   │   │   └── health/                 # 健康检查与状态端点
│   │   └── middlewares/                # 鉴权、限流、日志中间件
│   ├── core/                           # 核心业务逻辑层
│   │   ├── checker/                    # 链接可用性检测引擎（含重试与超时策略）
│   │   ├── scorer/                     # 质量评分计算模块（加权投票算法）
│   │   ├── feed/                       # RSS 生成器（支持 Atom 与 JSON Feed 格式）
│   │   └── snapshot/                   # 页面快照抓取与摘要提取
│   ├── db/                             # 数据库层
│   │   ├── migrations/                 # 数据库迁移脚本（按时间戳命名）
│   │   ├── models/                     # ORM 模型定义（Link, Collection, User, Vote, Tag）
│   │   └── seeders/                    # 开发环境初始数据填充
│   ├── services/                       # 外部服务集成层
│   │   ├── cache/                      # Redis 缓存封装（get/set/delete 及批量操作）
│   │   ├── queue/                      # 消息队列适配（Bull 实现，用于异步检测任务）
│   │   └── mail/                       # 邮件发送服务（验证码与通知）
│   ├── frontend/                       # 管理控制台前端（React + TypeScript）
│   │   ├── components/                 # 可复用 UI 组件（表格、卡片、筛选面板）
│   │   ├── pages/                      # 页面级组件（仪表盘、链接管理、收藏集详情）
│   │   ├── hooks/                      # 自定义 React Hooks（useFetch, useDebounce）
│   │   └── styles/                     # 主题变量与全局样式（含暗色模式支持）
│   └── utils/                          # 通用工具函数
│       ├── validator/                  # URL 校验、标签格式校验、输入清洗
│       ├── parser/                     # CSV/JSON 导入解析器
│       └── formatter/                  # 日期格式化、大小单位转换、状态码映射
├── tests/                              # 测试套件
│   ├── unit/                           # 单元测试（Jest + 模拟依赖）
│   ├── integration/                    # 集成测试（真实数据库与 Redis 实例）
│   └── e2e/                            # 端到端测试（Playwright 模拟用户操作）
├── docs/                               # 完整文档目录（见文档导航章节）
├── scripts/                            # 运维辅助脚本
│   ├── backup/                         # 数据库备份与恢复脚本
│   ├── deploy/                         # 容器编排配置文件（docker-compose 及 k8s 模板）
│   └── cron/                           # 定时任务脚本（每日检测、每周统计邮件）
├── .env.example                        # 环境变量配置模板
├── docker-compose.yml                  # 本地开发完整栈编排（PostgreSQL + Redis + app）
├── Dockerfile                          # 多阶段构建镜像定义
├── package.json                        # npm 项目清单与依赖声明
└── README.md                           # 项目入口文档（即本文档）
```

## 贡献指南

1. 阅读文档导航中的贡献规范与编码标准，确保本地开发环境满足安装要求，并运行 `npm run test` 确认现有测试全部通过后再行修改。

2. 在 GitHub Issues 中查找带有 `good first issue` 或 `help wanted` 标签的任务，或提交新 Issue 描述你希望解决的问题或新增功能，等待维护者回复确认。

3. Fork 本仓库并创建功能分支，分支命名遵循 `feature/描述` 或 `fix/描述` 格式，提交信息使用英文祈使句，单次提交保持逻辑原子化。

4. 完成修改后，补充或更新对应的单元测试与集成测试，确保测试覆盖率不低于现有基线（80%），并运行 `npm run lint` 和 `npm run format` 统一代码风格。

5. 发起 Pull Request 到主仓库的 `main` 分支，在 PR 描述中关联相关 Issue 编号，并逐项填写 PR 模板中的自检清单（测试通过、文档更新、无破坏性变更等）。维护者将在 3 个工作日内进行审查。

## 常见问题

**问：链接可用性检测是否会误判被反爬策略拦截的站点？**

答：检测引擎默认使用 Chrome 用户代理头，并跟随重定向，单次请求超时设为 10 秒。对于已知存在反爬机制的站点（如 Cloudflare 保护的页面），检测结果标记为“需人工复核”而非“失效”，同时记录响应头中的 `Server` 与 `CF-Ray` 信息供管理员判断。用户也可在链接设置中手动指定“跳过自动检测”或“仅检测域名可达性”。

**问：如何从旧版收藏夹或浏览器书签迁移数据？**

答：TechLink Navigator 提供导入向导，支持 Chrome 书签导出 HTML 文件、Firefox 书签 JSON 导出、以及通用 CSV 模板。导入时自动识别书签文件夹结构并转换为标签分类，重复链接依据 URL 完全匹配进行去重。对于无法解析的条目，系统会生成冲突报告供用户手动处理。批量导入任务在后台异步执行，完成后通过站内信通知。

**问：私有化部署是否支持 LDAP 或 OAuth 企业单点登录？**

答：从 v2.1.0 版本起，企业版部署配置支持 OIDC 协议，可对接 Okta、Azure AD、Keycloak 等身份提供商。LDAP 支持通过自定义策略扩展实现，具体配置参数详见运维手册中的 `authentication` 章节。社区版默认仅支持本地账号密码登录，不包含企业级 SSO 功能。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:34
