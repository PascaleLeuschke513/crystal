# VideoNexus 开源影视导航系统

VideoNexus 是一个面向技术开发者和内容整理者的开源影视资源导航与元数据聚合平台。项目定位为轻量级、可自托管的影视外链中心，专门解决个人或小型团队在整理、分类、分享合法影视资源链接时缺乏统一工具和结构化数据的问题。通过高度可定制的标签体系与多维度筛选能力，VideoNexus 帮助用户将散落的在线影视资源链接（如直播流、节目回放、正版片源等）转化为有序的、可检索的知识库，便于内部共享或社区公开访问。

本项目并非视频播放器或内容下载工具，不存储、不转码、不传播任何视频文件或盗版内容。它本质上是一个基于 Markdown 和静态 JSON 的链接管理框架，强调数据所有权归用户所有，部署简单，适合用于个人备忘、兴趣小组分享或合法公开内容聚合等非商业场景。

## 功能概览

- **多源链接收录与管理**：支持手动或批量导入影视资源 URL，自动提取网页标题与描述，形成统一资源条目。每个条目可自定义标签、分类、地区、语种等元数据。

- **灵活的标签与分类系统**：提供无限层级的分类树和自由标签云，允许按主题（如电影、剧集、纪录片）、地区（国产、欧美、日韩）、状态（连载中、已完结）等多维度整理资源，并支持组合条件的高级筛选。

- **内置全文检索与过滤**：基于资源标题、描述、标签、分类等字段实现毫秒级本地全文搜索，配合多选过滤组件，帮助用户在海量链接中快速定位所需资源。

- **响应式公共目录展示**：自动生成适合桌面与移动设备访问的资源列表页，支持列表视图与卡片视图切换。每个资源条目清晰显示来源网站名称、最后校验时间及访问状态。

- **资源健康度监控**：内置定时任务，可配置周期性对收录的 URL 进行可用性探测（HTTP 状态码检查），在管理后台中以图标或文本形式标记失效链接，辅助用户及时维护更新。

- **数据导入与导出标准化**：所有资源数据以 JSON 格式存储，支持完整的导入、导出功能，便于用户迁移数据、备份存档或与其他系统集成。导出格式兼容主流导航工具。

- **权限分级与协作支持**：提供基础的多用户角色管理（管理员、编辑者、访客），允许团队协作维护资源库。编辑者可新增、修改条目，但删除操作需管理员审核。

## 应用场景

- **个人影迷的观影清单整理**：用户可将各个流媒体平台、正版影视网站上的感兴趣节目链接集中收藏至 VideoNexus，按“想看”、“在看”、“已看”等自定义状态分类，配合个人评分与笔记，构建私人的影视档案库。

- **兴趣社区或论坛的资源索引**：小型电影爱好者论坛或字幕组社区，可使用 VideoNexus 搭建公开的“影视资源导航页”，将成员分享的合法预告片、花絮、官方观看渠道等链接分类整理，方便社区成员发现与访问。

- **内容创作者的外链工具箱**：视频博主或影视评论作者，可将创作过程中参考的原始资料、官方新闻稿、相关影评文章等链接统一收录，便于在撰写稿件或制作视频时快速引用，避免链接散落在浏览器收藏夹中。

- **企业内部的知识管理辅助**：对于媒体监测、市场分析等岗位，可将竞品动态、行业报告、发布会直播回放等业务相关的公开视频链接纳入系统，通过标签关联项目或客户，构建轻量级的企业情报追踪看板。

## 快速开始

以下步骤将引导您在本地环境中快速启动 VideoNexus 开发或测试实例。请确保已安装 Git 与 Node.js 环境。

```bash
# 1. 克隆项目仓库至本地
git clone https://github.com/videonexus/videonexus-core.git
cd videonexus-core

# 2. 安装项目依赖（使用 npm 或 yarn）
npm install
# 或
yarn install

# 3. 以开发模式启动应用，默认监听端口 3000
npm run dev
# 或
yarn dev
```

启动成功后，在浏览器中访问 `http://localhost:3000` 即可进入系统初始化页面。首次运行将引导您创建管理员账户。若需构建生产环境静态文件，可执行 `npm run build` 命令。

## 安装要求

在部署 VideoNexus 之前，请确保您的环境满足以下最低要求。推荐使用长期支持（LTS）版本的运行环境以获得最佳稳定性。

| 依赖项 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | v18.x 或更高 | 运行时基础环境，推荐使用 v20.x LTS 版本 |
| npm | v9.x 或更高 | 包管理器，与 Node.js 捆绑安装 |
| 操作系统 | Linux (Ubuntu 20.04+), macOS (12+), Windows (10/11) | 跨平台支持，生产环境推荐 Linux |
| 内存 | 最低 512 MB，推荐 1 GB | 内存占用随资源条目数量线性增长 |
| 存储空间 | 最低 200 MB | 用于存储程序文件、配置及 JSON 数据文件，不存储媒体文件 |
| 网络 | 稳定的互联网连接 | 用于初始安装依赖及资源 URL 健康度检查功能 |
| Git | v2.25 或更高 | 用于从代码仓库克隆项目源码 |

## 文档导航

为了帮助不同角色的用户快速找到所需信息，项目文档按面向人群和功能领域进行了分层组织。下表列出了核心文档模块及其解决的主要问题。

| 层面 | 目录/文档 | 回答的问题 |
| :--- | :--- | :--- |
| 用户指南 | `/docs/user-guide/` | 如何添加、编辑、分类资源？如何使用搜索和筛选？如何导入导出数据？ |
| 管理员手册 | `/docs/admin/` | 如何配置用户权限？如何设置定时任务？如何备份与恢复数据？ |
| 开发者文档 | `/docs/developer/` | 项目架构是怎样的？如何扩展 API 或自定义前端主题？如何参与贡献代码？ |
| 部署运维 | `/docs/deployment/` | 如何将系统部署到生产服务器（Nginx、PM2）？如何配置 HTTPS？如何迁移数据？ |

## 资源列表

本项目的正常运行与示例数据构建，参考或使用了以下公开的互联网影视资源导航站点。这些链接由社区用户整理提交，用于展示系统功能与分类结构的多样性。VideoNexus 项目本身与这些站点的运营方无任何从属或合作关系，不保证其内容的可用性与合法性，用户访问时应自行判断并遵守相关网站的使用条款。所有链接均按来源类别分组列出。

**直播与节目回放类**

*   <code>https://yijiabifenzhibo.org.cn</code>
*   <code>https://fajiabifenzhibo.org.cn</code>

**国产影视资源类**

*   <code>https://guochanjingpinzaixianmianfeikan.org.cn</code>

**字幕与影视资讯类**

*   <code>https://zhongwenzimuzaixianyingshiyuan.org.cn</code>

**免费观看聚合类**

*   <code>https://mianfeiguankanzaixianguankan.org.cn</code>
*   <code>https://jiujiushipinzaixianguankan.org.cn</code>

**欧美影视资源类**

*   <code>https://oumeizaixianguankanshipin.org.cn</code>

## 项目结构

项目采用模块化的设计思想，将核心业务逻辑、数据持久化、用户界面和系统配置进行清晰分离。以下为项目主要目录结构及其功能说明。

```
videonexus-core/
├── data/                           # 数据存储目录（默认，可配置）
│   ├── resources.json              # 核心资源条目数据（JSON格式）
│   ├── categories.json             # 分类树结构数据
│   ├── tags.json                   # 标签云数据
│   └── users.json                  # 用户账户与权限信息（加密存储）
├── src/                            # 源代码主目录
│   ├── api/                        # 后端API路由与控制器
│   │   ├── routes/                 # 具体路由定义文件
│   │   └── middleware/             # 身份验证、日志等中间件
│   ├── core/                       # 核心业务逻辑层
│   │   ├── resource-manager.js     # 资源条目的增删改查与校验
│   │   ├── search-engine.js        # 本地全文检索引擎实现
│   │   └── health-checker.js       # 链接可用性监控服务
│   ├── frontend/                   # 前端用户界面源码
│   │   ├── components/             # Vue/React 可复用UI组件
│   │   ├── views/                  # 页面级视图组件
│   │   ├── store/                  # 前端状态管理（如Pinia/Vuex）
│   │   └── assets/                 # 静态资源（CSS, 图片, 字体）
│   └── utils/                      # 通用工具函数库
│       ├── validator.js            # URL、邮箱等格式校验
│       ├── formatter.js            # 日期、文本格式化
│       └── fetcher.js              # 封装HTTP请求
├── config/                         # 项目配置文件目录
│   ├── default.json                # 默认配置（端口、数据库路径等）
│   ├── development.json            # 开发环境配置覆盖
│   └── production.json             # 生产环境配置覆盖（敏感信息通过环境变量注入）
├── tests/                          # 单元测试与集成测试文件
│   ├── unit/                       # 独立模块单元测试
│   └── integration/                # API及数据流集成测试
├── docs/                           # 项目文档（参见文档导航章节）
├── scripts/                        # 辅助脚本（数据库迁移、数据种子等）
├── .env.example                    # 环境变量模板文件
├── package.json                    # npm依赖管理及脚本定义
├── README.md                       # 项目主说明文档（即本文档）
└── LICENSE                         # MIT许可证文本
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，无论是报告问题、提出新功能建议、完善文档还是提交代码补丁。请遵循以下流程以高效协作。

1.  **查找或创建议题**：在开始工作前，请先在项目的 GitHub Issues 页面搜索是否已有相关议题。若无，请创建一个新议题，清晰描述您发现的问题或建议的新功能，等待核心维护者的反馈与确认。
2.  **派生项目并创建分支**：将主仓库派生（Fork）至您的个人账户，然后克隆到本地。请基于最新的 `main` 分支创建一个新的功能分支，分支名应简洁描述您的工作内容，例如 `fix-search-error` 或 `add-import-export-feature`。
3.  **编写代码与测试**：在您的分支上进行开发。请确保代码风格与项目现有规范保持一致，并为新增或修改的核心逻辑编写相应的单元测试，保证测试通过率不低于原有水平。
4.  **提交变更与发起合并请求**：完成开发后，将您的分支推送到 GitHub 上的派生仓库，然后向主仓库的 `main` 分支发起合并请求（Pull Request）。请在请求描述中详细说明变更内容、目的及测试情况，并关联相关的议题编号。
5.  **代码审查与合并**：核心维护者或其他贡献者将审查您的代码。可能会提出修改意见，请积极响应并更新代码。审查通过后，您的代码将被合并至主分支。

## 常见问题

**问：VideoNexus 是否提供在线演示站点？**

答：目前项目团队未维护公开的、长期在线的演示实例，因为演示数据可能过时或包含测试内容，无法代表系统最佳状态。我们强烈建议您按照本文档的“快速开始”步骤在本地或自己的服务器上部署试用，以获得完整的体验和管理权限。您也可以通过项目文档中的截图和操作动图了解界面交互流程。

**问：系统内置的链接健康度检查会影响我收录网站的访问统计吗？**

答：健康度检查功能仅发起标准的 HTTP HEAD 或 GET 请求以获取响应状态码（如 200、403、404），模拟的是普通浏览器访问行为。检查频率由用户配置，默认每日一次。对于绝大多数网站，这种低频、小流量的请求不会对其服务器造成负担，也通常不会影响站长的访问统计系统（因为不加载页面中的图片、脚本等资源）。若您对某些特定网站有顾虑，可在配置中为其单独关闭检查。

**问：我可以将 VideoNexus 用于商业目的，如搭建付费会员制的资源导航站吗？**

答：VideoNexus 采用 MIT 许可证发布，这是一个非常宽松的协议。它允许您自由地使用、复制、修改、合并、发布、分发、再授权及销售软件的副本。这意味着您可以将本系统用于商业项目，包括搭建付费服务。但请注意，您使用本软件收集和展示的链接内容本身的版权与合规性，由您自行负责，与项目无关。另外，若您对源码进行修改，没有义务必须开源您的修改版本，但保留原始版权声明是必须履行的义务。

## 许可证

本项目采用 MIT 许可证进行开源。这意味着您可以在遵守许可证条款的前提下，自由地使用、修改、分发本软件，包括用于商业目的。完整的许可证文本请参见项目根目录下的 `LICENSE` 文件，或访问开源倡议组织（OSI）网站获取标准版本。核心要点是：本软件按“原样”提供，不提供任何形式的明示或暗示担保，包括但不限于适销性、特定用途适用性和非侵权性的担保。在任何情况下，作者或版权持有人均不对任何索赔、损害或其他责任负责。

> 外链数量: 7 | 生成时间: 2026-08-14 22:07:51
