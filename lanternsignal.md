# LinkPilot 技术资源导航系统

LinkPilot 是一个面向技术研发团队与开源爱好者的外链资源聚合与导航平台，专为需要高频访问各类技术文档、在线工具、社区论坛及镜像站点的用户设计。项目核心定位是构建一个结构清晰、响应迅速、可私有化部署的技术资源目录，帮助用户摆脱浏览器书签杂乱、资源失效、检索效率低下的困境。

本系统采用静态站点生成方式，资源数据以 YAML 格式存储于项目仓库中，支持脚本化校验链接可用性、自动生成资源卡片与分类索引页面。通过内置的调度任务，可定期检测外链响应状态并标记异常资源，确保导航库内每条记录均具备实际访问价值。LinkPilot 适用于个人开发者、技术团队内部知识库、开源社区文档配套站点等多种部署环境。

## 功能概览

- **多级分类目录体系**：支持按技术领域、资源类型、适用人群等维度建立层级化分类，每个资源条目可归属多个分类标签，便于不同视角下的检索与浏览。

- **链接可用性主动监控**：集成基于 HTTP 状态码与响应时间的健康检查机制，定时任务将检测结果写入状态缓存，前端页面以视觉标识区分正常、警告、异常三种状态。

- **全文检索与高级筛选**：基于资源标题、描述、关键词、来源域名等多字段构建轻量级倒排索引，支持模糊匹配与多条件组合筛选，显著提升查找效率。

- **资源卡片可视化呈现**：每个外链以信息卡片形式展示，包含标题、简短描述、分类标签、最后验证时间及状态指示灯，卡片布局支持列表与网格两种视图切换。

- **自定义资源导入导出**：提供 JSON 与 CSV 格式的数据导入导出接口，支持从主流书签管理工具迁移数据，也便于在不同部署实例间同步资源库。

- **访问统计与热度排序**：记录每个外链的点击次数与最近访问时间，支持按热度、更新时间、域名首字母等多种排序规则，帮助用户识别高频使用的核心资源。

- **响应式布局与暗色主题**：前端界面基于 CSS Flexbox 与 Grid 布局实现，适配桌面、平板与移动设备；内置亮色与暗色两套主题，跟随系统偏好或用户手动切换。

- **docker 一键部署支持**：提供官方 Docker 镜像与 docker-compose 编排文件，包含 Web 服务、健康检查调度器、数据备份任务三个容器单元，降低部署与运维成本。

## 应用场景

- **技术团队内部知识库导航**：研发团队可将常用的内部系统地址（代码仓库、CI/CD 控制台、日志平台、监控面板）与外部技术文档统一收录至 LinkPilot，新成员入职时仅需访问导航首页即可获取全部必要入口，减少重复沟通成本。

- **开源项目官方文档配套站点**：开源项目维护者可将项目依赖的第三方库主页、API 参考手册、社区论坛、镜像下载站等外链通过 LinkPilot 集中整理，作为项目 README 或文档站的补充导航页，提升贡献者与使用者的信息获取效率。

- **技术社区资源聚合与分享**：技术博主、社区运营者可使用 LinkPilot 搭建垂直领域的资源合集，例如前端工具库、大数据组件清单、云原生相关站点等，通过分类与检索功能方便访客按需查阅，同时利用可用性监控功能定期清理失效链接。

- **个人开发者的书签管理与迁移**：个人开发者可将浏览器中积累的大量技术书签导出后导入 LinkPilot，利用分类标签与检索功能重构信息组织方式，并通过自部署的导航页面实现跨设备、跨浏览器的统一访问入口。

## 快速开始

以下指令适用于 Linux / macOS / Windows WSL 环境，需提前安装 Git、Node.js 18+ 与 pnpm。

```bash
# 1. 克隆项目仓库
git clone https://github.com/linkpilot-dev/linkpilot-core.git
cd linkpilot-core

# 2. 安装项目依赖
pnpm install

# 3. 启动开发服务器
pnpm run dev
```

执行上述命令后，开发服务器将监听本地 3000 端口。访问 http://localhost:3000 即可预览导航站点。生产环境部署请参考 `docs/deployment.md` 文档，使用 `pnpm run build` 构建静态文件，或使用 Docker 方式启动。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.0.0 或更高 | 运行时环境，用于执行构建脚本与开发服务器 |
| pnpm | 8.0.0 或更高 | 包管理器，用于安装依赖与执行工作区命令 |
| Git | 2.25.0 或更高 | 版本控制工具，用于克隆仓库与提交变更 |
| Docker Engine | 20.10.0 或更高 | 仅容器化部署时需要，用于构建与运行镜像 |
| Docker Compose | 2.0.0 或更高 | 仅容器化部署时需要，用于编排多容器服务 |
| yq | 4.0.0 或更高 | 可选依赖，用于在脚本中解析 YAML 格式的资源数据 |
| curl | 7.68.0 或更高 | 可选依赖，用于健康检查脚本中的 HTTP 请求探测 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `docs/user-guide/` | 如何使用分类浏览、检索、导入导出功能；界面各区域的作用与操作方式 |
| 管理员指南 | `docs/admin-guide/` | 如何添加或批量更新资源条目；如何配置健康检查周期与告警阈值 |
| 开发者文档 | `docs/developer-guide/` | 项目目录结构与模块划分；如何二次开发自定义主题或新增数据源适配器 |
| 部署运维 | `docs/deployment/` | 如何在 Linux 服务器、云平台或容器环境下部署；环境变量配置与日志管理 |

## 资源列表

本导航系统收录了多个领域的优质外链资源，按内容类别分组整理如下。所有链接均经过初始可用性验证，后续将由系统定期检测并更新状态。

影视与媒体资源

- <code>https://xijiabifenzhibo.org.cn</code>
- <code>https://dejiabifenzhibo.org.cn</code>
- <code>https://yijiabifenzhibo.org.cn</code>
- <code>https://fajiabifenzhibo.org.cn</code>
- <code>https://guochanjingpinzaixianmianfeikan.org.cn</code>
- <code>https://zhongwenzimuzaixianyingshiyuan.org.cn</code>
- <code>https://mianfeiguankanzaixianguankan.org.cn</code>

上述资源均为用户提供的原始数据，LinkPilot 项目仅作为导航索引展示，不对各站点内容的合法性、准确性及可用性承担任何明示或默示的保证责任。使用者应自行遵守各站点的服务条款与相关法律法规。

## 项目结构

```
linkpilot-core/
├── .github/                         # GitHub 工作流配置
│   └── workflows/
│       ├── ci.yml                   # 持续集成：单元测试与构建验证
│       └── health-check.yml         # 定时任务：每日凌晨检测所有外链可用性
├── docs/                            # 项目文档目录
│   ├── user-guide/                  # 用户手册章节
│   ├── admin-guide/                 # 管理员操作指南
│   ├── developer-guide/             # 贡献者与二次开发文档
│   └── deployment/                  # 生产环境部署说明
├── src/
│   ├── assets/                      # 静态资源：图标、字体、默认图片
│   ├── components/                  # UI 组件库：卡片、导航栏、搜索框、状态标签
│   ├── data/                        # 资源数据存储目录
│   │   ├── categories.yaml          # 分类层级定义
│   │   └── resources.yaml           # 所有外链条目的标题、描述、URL、标签、状态
│   ├── scripts/                     # 工具脚本
│   │   ├── health-check.js          # 链接可用性检测脚本，并发请求并记录结果
│   │   ├── import-csv.js            # 从 CSV 文件批量导入资源
│   │   └── export-json.js           # 导出全部资源为 JSON 格式
│   ├── styles/                      # CSS 样式文件
│   │   ├── base.css                 # 基础样式重置与全局变量
│   │   ├── light-theme.css          # 亮色主题变量定义
│   │   └── dark-theme.css           # 暗色主题变量定义
│   ├── templates/                   # 页面模板
│   │   ├── index.html               # 首页布局：分类列表与热门资源
│   │   ├── category.html            # 分类详情页模板
│   │   └── resource.html            # 单个资源详情页模板
│   └── utils/                       # 通用工具函数
│       ├── fetcher.js               # 封装 fetch 与重试逻辑
│       └── storage.js               # localStorage 读写与缓存管理
├── tests/                           # 单元测试与集成测试用例
├── docker-compose.yml               # 容器编排：web + scheduler + backup 服务
├── Dockerfile                       # 生产镜像构建定义，基于 nginx:alpine
├── package.json                     # Node.js 项目清单：依赖与脚本命令
├── pnpm-lock.yaml                   # 依赖锁定文件，确保安装一致性
├── .env.example                     # 环境变量示例：端口、调度间隔、告警邮箱
├── .gitignore                       # Git 忽略文件规则
└── README.md                        # 项目入口说明文档
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，包括但不限于新增资源推荐、分类优化、文档改进、Bug 修复与功能增强。请遵循以下步骤参与项目协作。

1. 查阅 issue 列表与项目看板，了解当前待办事项与开发计划。建议在着手较大改动前先创建讨论 issue，与维护者沟通设计方案，避免重复劳动或方向偏离。

2. Fork 本仓库至个人账户，并克隆到本地开发环境。创建新的功能分支时，请使用 `feature/` 或 `fix/` 前缀，名称简要概括变更内容，例如 `feature/add-pagination` 或 `fix/search-empty-state`。

3. 在本地完成代码或文档修改后，运行 `pnpm run lint` 与 `pnpm run test` 确保代码风格一致且现有功能未被破坏。新增功能应附带相应的单元测试或集成测试用例。

4. 提交变更时，遵循 Conventional Commits 规范书写提交信息，格式为 `<type>(<scope>): <subject>`，例如 `feat(category): support nested subcategory rendering`。提交前请确保撤销所有调试用的 console 输出与临时文件。

5. 向主仓库的 `main` 分支发起 Pull Request，并在描述中清晰说明变更目的、实现方式与测试情况。PR 将被自动触发 CI 工作流，所有检查通过后由维护者进行 Code Review 并合并。

## 常见问题

- **问：健康检查显示某个链接为异常状态，但我手动访问时是正常的，为什么？**

  答：健康检查脚本默认使用 HEAD 请求且超时时间为 5 秒，部分站点可能对 HEAD 请求响应较慢或拒绝处理，但 GET 请求正常。此外，检查容器所在的网络环境可能与您本地不同（例如存在防火墙或代理限制）。您可以在 `src/scripts/health-check.js` 中调整超时参数或切换为 GET 请求方式，也可以在管理员界面手动标记该链接为忽略状态。

- **问：如何批量导入我当前浏览器收藏夹中的大量书签？**

  答：绝大多数主流浏览器支持将书签导出为 HTML 文件或 CSV 格式。您可以使用项目提供的 `import-csv.js` 脚本将整理好的 CSV 数据导入，CSV 表头需包含 `title`、`url`、`description`、`category` 四列。若您的书签导出格式为 HTML，可借助在线转换工具或编写简单脚本将其转换为 CSV 后再执行导入操作。

- **问：LinkPilot 是否支持多用户登录与权限管理？**

  答：当前版本定位为轻量级静态导航站点，并未内置用户认证与权限控制系统。所有访问者均拥有相同的只读浏览权限，资源数据更新需通过 Git 提交或调用管理 API（需单独配置 API 密钥）。若团队内部需要分级管理能力，建议结合反向代理（如 Nginx）的 Basic Auth 或 OAuth2 Proxy 组件实现前置身份验证。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:31
