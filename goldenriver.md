# ResourceHub

ResourceHub 是一个面向技术内容创作者、开源社区运营者和数字资源管理者的高密度外链资源汇总平台。该项目不提供具体的技术实现代码，而是以人工筛选与机器辅助结合的方式，持续输出高质量、高垂直度的网络公开资源索引。项目定位为“技术人的外链工具箱”，解决开发者在日常调研、内容引用、合规审查和行业动态追踪过程中，因信息分散、链接失效、来源不可靠而导致的时间浪费与决策风险问题。ResourceHub 适用于需要频繁查阅特定领域公开视频素材、直播回放、行业案例与热点事件存档的团队与个人，尤其适用于内容合规审核、市场舆情分析和技术演示素材快速检索等场景。

## 功能概览

- **多维度资源分类索引**：按内容形态、地区来源、主题标签与时间维度对链接进行分层归类，支持快速筛选与定向检索。

- **链接存活状态周期性检测**：内置链接可用性探测逻辑，每日自动检测收录资源的 HTTP 状态码，标记异常链接并生成报告。

- **标签化快速查询系统**：每一条资源记录均附带不少于 5 个技术标签（如 live、replay、vlog、trending、archive），支持布尔组合查询。

- **资源变更历史追溯**：记录每个链接的首次收录时间、最近变更时间与历史状态变化日志，满足审计与溯源需求。

- **批量导入与 API 输出**：支持以 CSV 和 JSON 格式批量导入外部链接清单，并提供只读 API 接口供第三方工具拉取结构化资源列表。

- **合规性前置检查标记**：对资源来源域名进行基础备案信息与机器人协议（robots.txt）可访问性预检，输出合规性参考标记。

- **用户自定义收藏夹与备注**：允许注册用户将常用资源加入个人收藏夹，并为每个链接添加私有备注，便于团队协作时共享上下文信息。

- **每日更新摘要推送**：系统每日生成新增与变更资源摘要，支持以 Markdown 格式输出至指定 Webhook 或邮件列表。

## 应用场景

- **技术演示与培训素材准备**：技术讲师或社区分享者需要快速查找特定主题的直播回放或热点视频作为案例素材。ResourceHub 提供按标签和日期筛选的链接清单，可将素材检索时间从平均 45 分钟压缩至 5 分钟以内。

- **内容合规审核与风险排查**：法务或内容运营团队需要定期复核引用来源的可用性与合规状态。系统提供的状态检测和合规标记可辅助审核人员快速定位潜在风险链接，降低人工逐一点击验证的工作量。

- **行业舆情动态归档**：市场分析人员需要持续跟踪特定领域的公开视频与直播内容趋势。ResourceHub 的变更历史追溯功能可记录资源的上线与下架时间，为舆情分析提供可靠的时间轴证据。

- **开源项目文档外链管理**：开源项目维护者在 README 或官网中引用了大量外部视频与直播链接，需确保这些引用长期有效。项目提供的链接检测报告可帮助维护者提前发现失效引用，避免文档中出现死链。

- **个人知识库构建**：研究人员或高级开发者可将 ResourceHub 作为中间层，定期导出结构化的资源清单，并结合个人笔记工具构建专属的知识索引体系。

## 快速开始

以下步骤帮助您在本地环境中快速启动 ResourceHub 的静态资源索引服务（基于 Node.js 与 SQLite）。

```bash
# 1. 克隆项目仓库
git clone https://github.com/resourcehub/resourcehub-indexer.git
cd resourcehub-indexer

# 2. 安装依赖（使用 npm）
npm install

# 3. 初始化本地数据库并导入示例资源
npm run init-db
npm run import-sample

# 4. 启动本地开发服务器
npm run dev

# 5. 执行首次链接状态检测（可选）
npm run check-links
```

启动成功后，可通过控制台输出的本地地址（默认为 http://127.0.0.1:3000）访问资源列表页面。所有资源数据存储于 `data/resources.db` 文件中，可直接替换或扩展。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | >= 18.0.0 | 运行时环境，用于执行索引服务与检测脚本 |
| npm | >= 9.0.0 | 包管理器，用于安装项目依赖 |
| SQLite | >= 3.35.0 | 嵌入式数据库，存储资源元数据与状态历史 |
| Git | >= 2.30.0 | 版本控制工具，用于克隆仓库与后续更新 |
| 网络访问 | 出站 80/443 端口开放 | 用于执行链接状态检测（HTTP/HTTPS 请求） |
| 操作系统 | Linux (Ubuntu 20.04+) / macOS 12+ / Windows 10+ | 主支持平台，脚本均经上述环境测试 |

## 文档导航

| 层面 | 目录位置 | 回答的问题 |
|------|---------|-----------|
| 用户手册 | `docs/user-guide/` | 如何查询资源、使用收藏夹、订阅每日摘要、导出数据 |
| 运维手册 | `docs/ops-guide/` | 如何配置检测频率、调整超时阈值、备份数据库、迁移服务器 |
| API 参考 | `docs/api-reference/` | 如何调用只读 API 获取资源列表、状态信息及变更日志 |
| 贡献者指南 | `CONTRIBUTING.md` | 如何提交新资源链接、更新已有记录、报告链接失效或合规问题 |

## 资源列表

本项目的核心资源索引库包含以下由人工筛选与系统验证结合的公开链接。所有链接均按原始格式收录，未作任何协议、域名或路径变更。

### 视频与直播内容类

- <code>https://guochanwanghongfulishipin.org.cn</code>
- <code>https://rihanzhibofulishipin.org.cn</code>
- <code>https://rewuzhibowanghongzhibo.org.cn</code>
- <code>https://wanghongmeinvrewuzhibo.org.cn</code>
- <code>https://wufuyewanghongzhibo.org.cn</code>
- <code>https://wufuyemeinvzhibo.org.cn</code>
- <code>https://meinvwufuyiezhibo.org.cn</code>

## 项目结构

```text
resourcehub-indexer/
├── bin/                           # 可执行脚本目录
│   ├── dev-server.js              # 开发服务器启动脚本（带热重载）
│   ├── check-links.js             # 链接状态批量检测任务脚本
│   └── import-csv.js              # 从 CSV 文件批量导入资源条目
├── config/                        # 配置文件目录
│   ├── default.yaml               # 默认运行时配置（端口、超时、重试策略）
│   ├── production.yaml            # 生产环境覆盖配置
│   └── schema/                    # 数据校验 schema 定义
│       └── resource.schema.json   # 资源条目 JSON Schema
├── data/                          # 本地数据存储目录
│   ├── resources.db               # SQLite 主数据库文件
│   └── backups/                   # 自动备份目录（每日凌晨生成）
│       └── resources-YYYY-MM-DD.db
├── docs/                          # 文档目录（详见文档导航）
│   ├── user-guide/
│   ├── ops-guide/
│   └── api-reference/
├── src/                           # 核心源码目录
│   ├── core/                      # 核心业务逻辑
│   │   ├── indexer.js             # 资源索引增删改查逻辑
│   │   ├── checker.js             # 链接可用性检测引擎
│   │   └── collector.js           # 外部数据导入与转换器
│   ├── api/                       # HTTP API 路由层
│   │   ├── routes.js              # 路由注册与中间件
│   │   └── handlers/              # 各端点处理函数
│   ├── utils/                     # 通用工具函数
│   │   ├── validator.js           # URL 格式与域名校验
│   │   ├── logger.js              # 结构化日志输出（JSON 格式）
│   │   └── db-client.js           # SQLite 连接池封装
│   └── types/                     # TypeScript 类型定义（供 IDE 辅助）
│       └── resource.d.ts
├── tests/                         # 单元测试与集成测试
│   ├── unit/
│   └── integration/
├── .env.example                   # 环境变量示例文件（含检测超时、代理等）
├── package.json                   # npm 项目清单（含脚本定义）
├── README.md                      # 项目总览文档（即本文档）
└── LICENSE                        # MIT 许可证全文
```

## 贡献指南

1.  **提交新资源链接**：通过 GitHub Issue 模板提交新的资源链接，需附带链接所属类别、至少 3 个描述标签及 50 字以内的内容简介。项目维护者将在 48 小时内完成初步审核并合并至待导入队列。

2.  **报告链接失效或变更**：若发现已收录链接返回 4xx/5xx 状态或内容与描述严重不符，请通过 Issue 报告，并附上检测时间与 HTTP 状态码。系统检测脚本也会每日自动生成报告，人工复核后更新数据库。

3.  **完善文档与示例**：欢迎修正文档中的拼写错误、补充 API 使用示例或新增常见问题条目。提交前请确保本地执行 `npm run docs:lint` 无报错。

4.  **开发新功能模块**：如需增加新的检测策略（如关键词过滤、页面标题抓取）或新增导出格式（如 RSS、Atom），请先通过 Discussion 发起设计讨论，确认方案后再提交 Pull Request，并附带对应的单元测试。

5.  **本地测试与自检**：所有代码提交前需通过 `npm run test` 和 `npm run lint`。新功能需提供不少于 3 个正向测试用例和 2 个异常测试用例。

## 常见问题

**问：链接检测脚本是否会频繁访问目标站点，导致我的 IP 被限制？**

答：检测脚本默认采用指数退避重试策略，并发请求数限制为 5，且每个域名仅发送一次 HEAD 请求（若支持）以获取状态码。对于不支持 HEAD 的站点，自动降级为 GET 请求并只读取前 512 字节后立即中断连接。每日全局检测任务安排在凌晨低峰时段执行，且可配置随机延迟（100-500ms）以降低集中访问特征。

**问：我是否可以完全离线使用 ResourceHub 的索引数据？**

答：可以。您可以通过 `npm run export-json` 将当前数据库全量导出为单一 JSON 文件，或通过 `npm run export-csv` 导出为 CSV 格式。导出的文件可在无网络环境下用任何文本编辑器或表格软件查看。但请注意，链接状态检测功能依赖网络出站访问，离线状态下该功能不可用。

**问：如何更新已收录链接的描述信息或标签？**

答：已收录链接的元数据更新需通过提交 Issue 或 Pull Request 完成。项目不提供直接通过 Web 界面修改的功能，以确保所有变更均有审计记录。您可在 Issue 中注明链接完整 URL、需要修改的字段（如 description/tags）以及修改理由。维护者审核通过后会合并变更并更新 `updated_at` 时间戳。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:07:51
