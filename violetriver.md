# OpenLink 技术资源导航站

OpenLink 是一个面向开发者、技术研究者与内容运营人员的结构化外链与在线资源聚合平台。项目定位于对分散于互联网各处的视频直播、多媒体内容分发、实时流媒体互动站点进行系统性分类、可用性监测与访问导航，帮助用户快速定位目标资源，降低信息检索与站点筛选的时间成本。核心目标用户包括网络内容分析人员、流媒体技术选型工程师、行业数据采集从业者以及需要长期跟踪特定类别在线服务变化的研究型用户。

项目本身不存储、不转发、不缓存任何第三方媒体内容，仅提供公开可访问的 URL 索引与基础可用性元数据。通过对站点的响应时间、协议支持情况、内容类型标识等维度进行周期性检测，项目可为用户提供客观的链接状态参考，辅助判断资源的当前可用性与访问质量。

## 功能概览

- **多维度链接分类索引**：按内容类型、运营地域、语言、访问热度等标签体系对收录链接进行精细分类，支持多级筛选与组合查询，便于快速缩小目标范围。

- **可用性主动监测**：对收录的每个链接实施周期性的 HTTP/HTTPS 探测，记录状态码、响应时间、TLS 版本等指标，并以直观标识展示当前可用状态。

- **访问质量评分**：综合响应速度、连接稳定性、证书有效性等因素，为每个站点生成动态质量评分，辅助用户判断访问体验预期。

- **标签与全文检索**：支持基于站点名称、描述、标签、域名关键词的全文检索，检索结果按相关度与质量评分排序，提升查找效率。

- **个人收藏与自定义分类**：允许注册用户将常用链接加入收藏夹，并创建自定义分类目录，实现个性化资源组织。

- **链接变更历史追踪**：记录每个收录站点的标题、描述、响应状态等字段的历史变更，便于追踪资源内容的演变与迁移。

## 应用场景

- **流媒体技术选型参考**：技术团队在评估第三方流媒体服务提供商时，可通过本平台检索同类站点，了解其使用的技术栈、访问协议及性能表现，为选型决策提供背景信息。

- **内容合规性监测辅助**：内容审核与合规运营人员可利用平台定期检测相关站点的可访问性及内容类型标识，辅助监测合规边界变化，及时调整内部策略。

- **行业数据采集规划**：数据采集工程师在规划爬虫任务时，可通过本平台获取目标站点的响应特征（如状态码历史、重定向链、内容类型），制定更稳健的采集策略。

- **学术研究与行业报告**：研究机构在撰写网络内容生态、流媒体产业发展相关报告时，可利用平台提供的分类索引与状态数据作为样本筛选与趋势分析的参考依据。

## 快速开始

以下步骤将指导您在本地环境完成项目的克隆、依赖安装与服务启动。

```bash
# 1. 克隆项目仓库
git clone https://github.com/openlink-dev/openlink-hub.git
cd openlink-hub

# 2. 安装后端依赖（Python）
pip install -r requirements.txt

# 3. 安装前端依赖（Node.js）
cd frontend
npm install
npm run build
cd ..

# 4. 初始化数据库
python scripts/init_db.py

# 5. 启动开发服务
python app.py
```

服务启动后，访问本地端口（默认 5000）即可开始使用。生产环境部署请参考 `deploy` 目录下的运维文档。

## 安装要求

项目运行所需依赖组件及版本要求如下表所示。请确保部署环境已安装对应基础软件。

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Python | 3.9 及以上 | 后端服务运行环境，推荐使用 3.10 或 3.11 |
| Node.js | 18.x LTS 及以上 | 前端构建与开发服务器依赖 |
| PostgreSQL | 14.x 及以上 | 主要关系型数据库，存储链接元数据、用户信息、分类体系 |
| Redis | 6.x 及以上 | 缓存与会话存储，用于提升检索性能与用户态管理 |
| Nginx | 1.22 及以上 | 生产环境反向代理与静态资源服务（可选，但强烈推荐） |
| pip | 22.x 及以上 | Python 包管理工具，用于安装 requirements.txt 中的依赖 |
| npm 或 yarn | 8.x 及以上 | 前端包管理，用于安装与构建前端资源 |
| Git | 2.30 及以上 | 版本控制工具，用于克隆与更新代码仓库 |
| 操作系统 | Linux (Ubuntu 20.04+) 或 macOS 12+ | 开发与生产环境优先推荐 Linux 发行版，Windows 用户请使用 WSL2 |

## 文档导航

项目文档按使用角色与阅读目的划分为以下层级，建议新用户从「用户手册」开始阅读。

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户层面 | `docs/user/quick-start.md` | 如何注册账号、如何检索链接、如何添加收藏与自定义分类？ |
| 用户层面 | `docs/user/advanced-usage.md` | 如何使用高级筛选、导出检索结果、配置个性化通知？ |
| 运营层面 | `docs/operator/monitoring-guide.md` | 如何配置可用性监测周期、调整质量评分权重、查看监测日志？ |
| 运营层面 | `docs/operator/link-submission.md` | 如何提交新的链接资源、审核流程是什么、如何更新已有链接信息？ |
| 开发层面 | `docs/developer/api-reference.md` | 后端 RESTful API 的端点定义、请求参数、响应格式与鉴权方式是什么？ |
| 开发层面 | `docs/developer/architecture.md` | 项目的整体架构设计、模块划分、数据流向与技术选型理由是什么？ |
| 运维层面 | `docs/deploy/production-setup.md` | 如何在生产环境部署服务、配置 HTTPS、设置定时任务与日志轮转？ |
| 运维层面 | `docs/deploy/troubleshooting.md` | 常见部署与运行异常的排查步骤和解决方案有哪些？ |

## 资源列表

本项目作为技术资源导航站，对外收录并索引以下公开可访问的链接。所有链接均按用户提供的原始形式原样列出，未做任何协议补全、域名改写或大小写调整。收录链接的具体分类标签与详细元数据请通过平台检索界面查询。

### 直播与视频内容类

- <code>https://guochanwanghongshipinzhibow.org.cn</code>
- <code>https://wanghongzhibomianfeiguankanw.org.cn</code>
- <code>https://meinvzhibozaixiankanw.org.cn</code>
- <code>https://guochanwanghongfulishipinw.org.cn</code>
- <code>https://rihanzhibofulishipinw.org.cn</code>
- <code>https://rewuzhibowanghongzhibow.org.cn</code>
- <code>https://wanghongmeinvrewuzhibow.org.cn</code>

## 项目结构

项目采用前后端分离架构，后端基于 Python Flask 提供 RESTful API，前端基于 React 构建单页应用。核心目录结构与功能说明如下：

```
openlink-hub/
├── app/                           # 后端核心应用目录
│   ├── api/                       # RESTful API 路由与视图函数
│   │   ├── v1/                    # API v1 版本实现
│   │   │   ├── links.py           # 链接资源的 CRUD 与检索接口
│   │   │   ├── monitoring.py      # 可用性监测数据查询接口
│   │   │   └── users.py           # 用户注册、登录、收藏管理接口
│   │   └── __init__.py
│   ├── models/                    # 数据库模型定义（SQLAlchemy ORM）
│   │   ├── link.py                # 链接实体模型（含 URL、标题、标签、状态等字段）
│   │   ├── user.py                # 用户模型与认证相关字段
│   │   └── monitor_log.py         # 监测历史记录模型
│   ├── services/                  # 业务逻辑层
│   │   ├── checker.py             # 链接可用性检测服务（HTTP 探活、证书检查）
│   │   ├── scheduler.py           # 定时任务调度（周期性检测、数据清理）
│   │   └── indexer.py             # 全文索引构建与检索服务
│   ├── utils/                     # 工具函数与辅助模块
│   │   ├── validators.py          # URL 校验、输入清洗
│   │   └── parsers.py             # 响应内容解析与元数据提取
│   └── config.py                  # 环境配置（开发、测试、生产）
├── frontend/                      # 前端单页应用目录
│   ├── src/
│   │   ├── components/            # React 组件（导航栏、链接卡片、筛选面板等）
│   │   ├── pages/                 # 页面级组件（首页、检索页、详情页、个人中心）
│   │   ├── hooks/                 # 自定义 React Hooks（API 请求、状态管理）
│   │   └── utils/                 # 前端工具函数
│   ├── public/                    # 静态资源（favicon、manifest 等）
│   └── package.json               # 前端依赖声明与脚本配置
├── scripts/                       # 运维与开发辅助脚本
│   ├── init_db.py                 # 数据库初始化与种子数据加载
│   └── migrate_db.py              # 数据库结构迁移脚本
├── tests/                         # 单元测试与集成测试
│   ├── api/                       # API 接口测试用例
│   └── services/                  # 服务层逻辑测试用例
├── deploy/                        # 生产部署相关配置
│   ├── nginx.conf                 # Nginx 反向代理配置示例
│   ├── docker-compose.yml         # Docker Compose 快速编排文件
│   └── systemd/                   # systemd 服务单元文件
├── docs/                          # 完整项目文档（详见上方文档导航）
├── requirements.txt               # Python 后端依赖列表
├── README.md                      # 项目说明文件（即本文档）
└── LICENSE                        # MIT 许可证文件
```

## 贡献指南

开源社区的每一位贡献者都是项目持续演进的重要动力。我们欢迎问题反馈、功能建议、文档改进与代码提交。请遵循以下步骤参与贡献：

1. **提交 Issue 进行讨论**：在提交 Pull Request 之前，请先在 GitHub Issues 中创建对应议题，描述您发现的问题或希望新增的功能。核心维护者会在一周内给予反馈，确认方向可行后再进行开发，避免无效工作。

2. **Fork 仓库并创建特性分支**：从主仓库 Fork 代码后，请基于最新的 `main` 分支创建您的特性分支，分支命名建议采用 `feature/功能简述` 或 `fix/问题简述` 格式，便于识别。

3. **编写或更新测试用例**：任何代码变更都应同步更新或新增对应的单元测试与集成测试，确保测试覆盖率达到变动代码的 80% 以上。测试需在本地全部通过后方可提交。

4. **遵循代码规范与提交信息格式**：Python 代码请遵循 PEP 8 规范，前端代码遵循 ESLint 配置。提交信息请使用约定式提交格式（如 `feat: 增加链接批量导入接口`、`fix: 修复监测任务超时异常`），便于自动生成变更日志。

5. **发起 Pull Request 并等待 Code Review**：将您的特性分支推送到 Fork 仓库后，向主仓库的 `main` 分支发起 Pull Request。PR 描述中请关联对应的 Issue 编号，并简要说明变更内容与测试结果。至少一位核心维护者会进行代码审查，通过后即可合并。

## 常见问题

**Q1：平台检测到某个链接显示不可用，但我在浏览器中可以正常访问，是什么原因？**

A1：平台监测服务部署在特定的云服务器上，其网络出口可能受到地域限制、运营商策略或 IP 信誉影响。同时，部分站点会针对非浏览器请求返回非标准响应，或对同一 IP 的频繁探测实施访问限制。建议您先确认监测服务所在区域与您的访问环境是否一致，若差异较大，可通过平台提供的「手动验证」功能触发单次实时检测。若仍存在分歧，请在 Issue 中提交包含目标链接与访问时间的信息，维护团队会核查监测节点的配置与探测逻辑。

**Q2：我提交了新的链接资源，但迟迟未在平台上看到收录，审核周期是多久？**

A2：新提交的链接会进入人工审核队列，审核周期通常为 3 至 5 个工作日。审核内容包括链接的可用性、内容类别匹配度、是否违反收录准则（如涉及非法内容、恶意软件、频繁跳转等）。审核通过后，链接将在下一次元数据同步任务中（每日凌晨执行）正式上架。若您的提交超过 7 个工作日仍未处理，请在 Issue 中说明提交时间与链接信息，我们会优先跟进。

**Q3：项目是否支持部署在完全内网环境，不访问任何公网资源？**

A3：支持。项目核心代码与依赖包均可离线部署。您需要在内网环境中提前准备 Python、Node.js、PostgreSQL 与 Redis 的离线安装包，并将 `requirements.txt` 中的 PyPI 依赖与 `frontend/package.json` 中的 npm 依赖分别打包上传。部署时，请将 `config.py` 中的 `MONITOR_ENABLED` 配置项设置为 `False`，关闭对外部链接的自动探测任务，仅使用手动导入或批量录入方式管理链接数据。离线部署的详细步骤请参考 `docs/deploy/offline-setup.md`。

## 许可证

本项目采用 MIT 许可证。您可以在遵守许可证条款的前提下自由使用、修改、分发本项目的源代码，包括用于商业目的。完整的许可证文本请参见项目根目录下的 LICENSE 文件。

> 外链数量: 7 | 生成时间: 2026-08-14 22:07:51
