# HrefHub

HrefHub 是一个面向技术内容创作者、开源项目维护者以及数字化档案管理人员的轻量级外链资源聚合与导航系统。本项目并非传统的网址导航站，而是定位为“结构化外链资产的管理工具”，用于解决个人或团队在维护多个项目文档、技术博客或知识库时，外链分散、失效、难以追溯和审查的痛点。

HrefHub 以纯静态 Markdown 为数据源，通过简单的目录约定和脚本解析，将分散的 URL 资源按照项目、领域、批次进行归类，并生成统一的资源清单看板。其目标用户包括开源项目文档维护者、技术社区运营人员、以及需要长期管理大量参考链接的研究者。通过 HrefHub，用户能够以极低的维护成本，建立一套可审计、可回溯、可共享的外链资源体系。

## 功能概览

- **批量外链入库解析**：支持按批次导入原始 URL 列表，自动识别域名、协议及路径结构，并提取元数据用于后续分类与检索。
- **分类目录映射引擎**：基于用户定义的映射规则，将未分类的 URL 动态挂载至项目结构中的多个虚拟目录，实现一对多的资源视图。
- **资源状态健康检查**：内置链接可达性探测模块，可定期对已收录的 URL 执行 HEAD 请求，标记失效或重定向资源，并生成异常报告。
- **只读 Markdown 生成器**：将解析后的外链数据渲染为符合项目规范的 README 章节、资源列表及文档导航表格，确保输出内容始终为纯文本，无需数据库依赖。
- **审计日志追踪**：记录每次资源批次的新增、删除与修改操作，包括操作时间、批次号与变更摘要，便于团队协作时回溯历史状态。
- **多粒度检索与过滤**：支持按域名、关键词、批次号、资源类型等维度对已收录链接进行即时过滤，并提供正则表达式高级匹配模式。
- **导出与集成接口**：提供 JSON 与 YAML 格式的结构化数据导出功能，可被 CI/CD 流水线或文档生成工具链直接消费，减少重复人工录入。

## 应用场景

- **开源项目文档的多源参考维护**：当项目 README 或用户指南中需要引用大量第三方技术文章、官方规范或社区讨论帖时，维护者可将所有外链统一托管于 HrefHub，由系统自动生成格式规范的资源列表，避免手动编辑导致的格式混乱和链接漂移。
- **技术社区月度资源汇总**：社区运营人员每月收集成员分享的优质技术博文、视频教程或工具站点，通过 HrefHub 按批次录入，并自动生成带分类标签的资源合集，便于发布为月刊或周报的附件内容。
- **企业内部知识库外链治理**：企业内部 Wiki 中散布大量指向外部供应商、标准组织或学术数据库的链接。使用 HrefHub 可集中管理这些外链，并定期执行健康检查，及时通知维护人员更新失效地址，保障知识库的可靠性。
- **研究与档案项目的证据链存档**：学术研究或法律档案项目中，需保留大量网页快照及原始链接作为引用证据。HrefHub 提供严格的 URL 原样保存和批次追踪能力，确保任何时间点都能精确还原引用的原始来源，满足审计合规要求。

## 快速开始

以下步骤帮助您在本地环境中快速启动 HrefHub 实例，并完成第一批资源的导入与预览。

```bash
# 1. 克隆项目仓库至本地
git clone https://github.com/hrefhub/hrefhub-core.git
cd hrefhub-core

# 2. 安装依赖（基于 Python 3.10+）
pip install -r requirements.txt

# 3. 准备资源批次文件（CSV 或纯文本列表）
# 将待收录的 URL 按行放入 data/batch_10.txt
# 示例：echo "<code>https://yingchaobifenzhibo.org.cn</code>" >> data/batch_10.txt

# 4. 执行资源解析与生成
python src/main.py --batch 10 --input data/batch_10.txt --output docs/

# 5. 查看生成的资源清单
cat docs/resource_batch_10.md
```

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.10 及以上 | 核心解析与生成引擎依赖，较低版本可能不支持类型提示特性 |
| Git | 2.25 及以上 | 用于克隆仓库和管理版本，非运行时期必需但推荐 |
| pip | 21.0 及以上 | Python 包管理工具，用于安装 requirements.txt 中的第三方库 |
| markdown | 3.4 及以上 | 用于渲染和验证生成的 Markdown 结构，确保输出符合 CommonMark 规范 |
| pytest | 7.0 及以上 | 仅开发测试环境需要，用于执行单元测试与集成测试用例 |
| requests | 2.28 及以上 | 用于链接健康检查模块的 HTTP 请求发送，支持超时与重试配置 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | docs/user_guide/ | 如何安装、配置批次文件、执行资源导入和生成输出？ |
| 管理员指南 | docs/admin_guide/ | 如何自定义分类映射规则、调整健康检查策略、备份数据？ |
| 开发参考 | docs/dev_reference/ | 核心模块的接口定义、插件扩展机制、以及 CI 集成示例？ |
| 设计文档 | docs/design/ | 系统整体架构设计、数据流、以及为何选择纯静态方案的技术考量？ |
| 常见任务 | docs/tasks/ | 如何迁移旧有链接数据、如何批量替换域名、如何处理重定向链？ |

## 资源列表

### 当前批次（第 10/130 批）

本批次共收录 7 个资源链接，归类为“技术参考与信息站点”。所有链接均按原始格式原样保存，未进行任何协议升级、域名规范化或路径改写。

#### 信息参考类

<code>https://yingchaobifenzhibo.org.cn</code>

<code>https://xijiabifenzhibo.org.cn</code>

<code>https://dejiabifenzhibo.org.cn</code>

<code>https://yijiabifenzhibo.org.cn</code>

<code>https://fajiabifenzhibo.org.cn</code>

#### 多媒体资源类

<code>https://guochanjingpinzaixianmianfeikan.org.cn</code>

<code>https://zhongwenzimuzaixianyingshiyuan.org.cn</code>

## 项目结构

```
hrefhub-core/
├── src/                           # 核心源代码目录
│   ├── parser/                    # URL 解析与元数据提取模块
│   │   ├── url_processor.py       # 处理协议、域名、路径拆分
│   │   └── batch_loader.py        # 加载批次文件并校验格式
│   ├── generator/                 # 输出生成模块
│   │   ├── markdown_builder.py    # 构建 README 与资源列表章节
│   │   └── exporter.py            # 导出 JSON/YAML 格式数据
│   ├── checker/                   # 链接健康检查模块
│   │   ├── probe.py               # 异步 HTTP 探测任务
│   │   └── reporter.py            # 生成失效链接报告
│   ├── core/                      # 核心数据模型与配置
│   │   ├── models.py              # Resource, Batch, Category 等数据类
│   │   └── config.py              # 全局配置项读取与管理
│   └── main.py                    # 命令行入口，协调各模块执行
├── tests/                         # 单元测试与集成测试目录
│   ├── test_parser/               # 解析模块测试用例
│   ├── test_generator/            # 生成模块测试用例
│   └── test_checker/              # 检查模块测试用例
├── docs/                          # 生成的文档输出目录（示例）
│   ├── resource_batch_10.md       # 第 10 批次资源清单
│   └── index.md                   # 项目整体资源索引
├── data/                          # 批次原始数据存放目录
│   └── batch_10.txt               # 当前批次原始 URL 列表
├── requirements.txt               # Python 依赖列表
└── README.md                      # 项目主说明文档（当前文件）
```

## 贡献指南

我们欢迎并鼓励社区贡献，无论是报告问题、改进文档，还是新增解析插件。请遵循以下步骤参与贡献：

1. 查阅现有议题：访问 GitHub Issues 页面，确认您要解决的问题或建议的功能尚未被他人认领或实现。若无相关议题，请先创建新议题并等待维护者反馈。
2. 派生并克隆仓库：点击项目页面右上角的 Fork 按钮，将项目派生至个人账户，随后克隆至本地开发环境。
3. 创建功能分支：请基于 main 分支创建新的分支，分支命名格式为 `feature/简要描述` 或 `fix/问题编号`，例如 `feature/support-ftp-protocol`。
4. 编写代码与测试：提交代码前，请确保新功能或修复包含对应的单元测试，并执行 `pytest tests/` 确认所有测试用例通过。同时更新相关文档章节。
5. 发起合并请求：推送分支至个人远程仓库，随后向主仓库的 main 分支发起合并请求。请在请求描述中清晰引用相关议题编号，并简述更改内容与测试结果。

## 常见问题

**问：HrefHub 是否支持非 HTTPS 协议的链接，例如 HTTP 或 FTP？**

答：支持。HrefHub 在设计上对协议类型不做硬性限制，解析器会保留原始输入的协议前缀。但链接健康检查模块默认仅对 HTTP/HTTPS 协议执行探测，FTP 或其他自定义协议将跳过检查并记录为“未探测”。如需扩展，用户可继承 `checker.probe.BaseProbe` 类实现自定义探针。

**问：如果批量导入的链接中存在重复条目，系统如何处理？**

答：系统默认以“域名+路径+查询参数”作为唯一性判据。在同一批次内重复的 URL 会被自动去重，仅保留首次出现记录。跨批次出现的重复链接不会自动删除，但会在生成资源清单时添加重复标记，并记录所有引用批次号，方便用户决策是否合并。

**问：如何迁移已有的书签文件（如 Chrome 导出的 HTML 书签）至 HrefHub？**

答：HrefHub 未内置直接解析浏览器书签 HTML 的功能，但您可以将书签链接逐一提取为纯文本列表（每行一个 URL），保存为批次文件，即可通过标准导入流程处理。我们建议使用第三方书签转换工具先将书签转为 CSV 格式，再利用 HrefHub 的 CSV 批次导入接口（参见开发参考中的扩展模块）。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:07:51
