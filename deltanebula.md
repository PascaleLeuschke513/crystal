# NexusIndex

NexusIndex 是一个面向技术内容创作者、本地化工程师与数字档案管理者的外链资源归集与元数据导航系统。该项目并非传统的爬虫或采集工具，而是一套以人工审核为基线的外链索引管理框架，帮助用户在信息过载的环境中快速定位高价值外部资源，并建立可维护的资源清单体系。项目主要服务于需要定期整理、分类、校验外部链接的技术文档团队、开源社区运营者以及个人知识管理实践者，通过标准化的索引模板与自动化健康检查，降低链接失效与资源错配带来的维护成本。

## 功能概览

- **多协议链接归一化存储**：系统自动识别并存储包含 HTTP 与 HTTPS 协议的资源地址，保留原始协议头与域名结构，确保跳转行为的完全一致性。

- **域名批次管理与标签化分类**：支持按导入批次、内容主题、语言属性、区域来源对资源进行多维标签绑定，批次 86/130 标识当前资源集所属的迭代周期。

- **资源可用性主动探测**：内置轻量级链接校验引擎，定期发送 HEAD 请求检测目标资源可达性，自动标记异常链接并生成告警日志。

- **外链元数据扩展字段**：每条资源记录支持附加备注、内容摘要、最后验证时间、响应状态码等元数据，为后续分析与筛选提供结构化依据。

- **只读导航页生成器**：基于存储的资源列表与分类标签，一键生成只读模式的 Markdown 导航页，便于嵌入项目文档或静态站点侧边栏。

- **变更审计追踪**：记录每次资源新增、删除或属性修改的操作人、时间戳与变更原因，满足团队协作场景下的可追溯要求。

- **批量导入导出接口**：支持 CSV 与 JSON 格式的资源列表批量导入与导出，兼容主流电子表格软件与数据处理脚本。

## 应用场景

- **开源文档本地化团队的参考链接管理**：本地化工程师在翻译技术文档时，需要频繁引用外部术语库、风格指南或参考实现。NexusIndex 允许团队预先归集一批经过验证的参考链接，并为每条链接标注适用范围与审核状态，减少翻译过程中的外部搜索时间。

- **社区运营者的每周资源精选发布**：技术社区运营者每周需要整理用户推荐的优质外链，形成周报或月刊。使用 NexusIndex 的批次标签功能，可快速为第 86 批资源生成独立导航页，并通过项目内置的模板渲染为社区公告。

- **个人知识库的外部引用固化**：知识管理实践者在构建长期笔记体系时，往往面临外部链接年久失修的问题。NexusIndex 的主动探测功能可在每月巡检中自动报告失效链接，提醒用户更新或归档，保障知识库的长期可靠性。

- **档案数字化项目中的源地址登记**：在古籍数字化、多媒体档案整理等场景中，工作人员需要登记大量外部源地址。项目提供的元数据扩展字段支持记录来源机构、数字化日期、版权状态等信息，形成规范的资源登记台账。

## 快速开始

以下命令适用于 Linux 与 macOS 环境，Windows 用户建议通过 WSL2 或 Git Bash 执行。

```bash
# 克隆项目仓库
git clone https://github.com/nexusindex/core.git nexusindex
cd nexusindex

# 安装依赖（Python 3.10+ 与 pip）
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 初始化本地数据库并导入示例资源
python scripts/init_db.py
python scripts/import_batch.py --batch 86 --source data/batch_86.json

# 启动开发导航服务
python app.py --port 8080
```

访问本地 `http://127.0.0.1:8080/` 即可查看当前批次的资源导航页。生产环境部署请参考 `docs/deployment.md`。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.10 至 3.12 | 核心运行环境，低于 3.10 将无法兼容类型注解语法 |
| SQLite | 3.35.0 及以上 | 内置数据库引擎，用于存储资源条目与元数据 |
| pip | 22.0 及以上 | Python 包管理工具，用于安装依赖库 |
| Git | 2.30.0 及以上 | 版本控制工具，用于克隆仓库与管理补丁 |
| curl | 7.68.0 及以上 | 链接探测模块依赖的命令行工具，用于发送 HTTP 请求 |
| make | 3.81 及以上 | 可选，用于执行自动化脚本与测试套件 |

## 文档导航

| 层面 | 目录文件 | 回答的问题 |
|---|---|---|
| 用户手册 | `docs/user-guide.md` | 如何导入新批次资源？如何编辑已有资源的元数据？如何生成导航页？ |
| 管理员指南 | `docs/admin-guide.md` | 如何配置探测间隔与告警阈值？如何备份与恢复数据库？如何迁移至 PostgreSQL？ |
| 开发者文档 | `docs/developer-guide.md` | 如何扩展自定义探测协议？如何增加新的标签分类器？如何运行单元测试？ |
| 设计决策 | `docs/design-decisions.md` | 为什么选择 SQLite 作为默认存储？为什么保留原始协议头而不做归一化？批次号的设计含义是什么？ |

## 资源列表

按内容主题划分，本批次共收录 7 个外链资源，均为中文域名体系下的内容型站点。

影视与多媒体资源类

<code>https://renqixiliezhongwenzimuw.org.cn</code>

<code>https://rihanmeinvzhongwenzimu.org.cn</code>

<code>https://qingqingcaoyuanzhongwenzimu.org.cn</code>

<code>https://guochanjingpinzaixianmianfeikanb.org.cn</code>

<code>https://zhongwenzimuzaixianyingshiyuanb.org.cn</code>

<code>https://mianfeiguankanzaixianguankanb.org.cn</code>

<code>https://jiujiushipinzaixianguankanb.org.cn</code>

## 项目结构

```
nexusindex/
├── app.py                         # 主应用程序入口，启动导航服务与 API 端点
├── requirements.txt               # Python 依赖清单，包含 Flask、requests、pytest 等
├── config/
│   ├── settings.py                # 全局配置项，含数据库路径、探测超时、批次号
│   └── logging.conf               # 日志格式与输出级别配置
├── core/
│   ├── __init__.py
│   ├── storage.py                 # SQLite 数据库 CRUD 操作封装
│   ├── probe.py                   # 链接可用性探测引擎，调用 curl 执行 HEAD 请求
│   ├── tags.py                    # 标签分类逻辑与批次管理
│   └── exporter.py                # JSON / CSV 格式导入导出实现
├── scripts/
│   ├── init_db.py                 # 初始化数据库表结构
│   ├── import_batch.py            # 按批次导入资源列表，支持 --source 参数
│   └── health_check.py            # 手动触发全量链接健康检查
├── tests/
│   ├── test_storage.py            # 存储层单元测试
│   ├── test_probe.py              # 探测模块模拟测试
│   └── fixtures/                  # 测试用固定数据集
├── docs/                          # 完整文档目录，对应上文文档导航
│   ├── user-guide.md
│   ├── admin-guide.md
│   ├── developer-guide.md
│   └── design-decisions.md
├── templates/
│   └── nav.html                   # 导航页渲染模板，使用 Jinja2 语法
└── README.md                      # 当前文件
```

## 贡献指南

1. **阅读设计决策文档**：在提交任何代码或资源变更前，请先阅读 `docs/design-decisions.md`，理解本项目对链接存储、协议处理和批次编号的核心原则，确保您的贡献与项目哲学一致。

2. **创建议题讨论变更**：对于新增功能、修改现有行为或批量资源更新，请在 GitHub Issues 中创建一个议题，简要描述变更目的、影响范围与测试计划。对于微小修正（如文档错别字），可直接发起拉取请求。

3. **遵循代码风格与测试要求**：所有 Python 代码必须通过 `flake8` 与 `pylint` 基础检查，且新增逻辑需附带至少一个单元测试用例。运行 `make test` 可执行完整测试套件，确保本地环境已通过全部测试后再提交。

4. **提交拉取请求并关联议题**：分支命名建议采用 `feature/` 或 `fix/` 前缀，拉取请求描述中请使用 `Closes #ISSUE_NUMBER` 关联先前创建的议题。请求合并前请确保目标分支为 `main`，且提交历史保持整洁（允许 rebase）。

5. **资源列表更新规范**：若贡献涉及资源列表的新增、删除或元数据修改，请同步更新 `data/` 目录下的对应批次 JSON 文件，并在提交信息中注明批次号与变更原因。严禁直接编辑 README 中的资源列表章节，所有资源变更必须通过导入脚本完成。

## 常见问题

**问：资源列表中的域名为什么保留了 HTTP 或 HTTPS 协议头？项目是否会自动将 HTTP 重定向为 HTTPS？**

答：项目强制保留用户输入的原始协议头，不做任何自动改写。这是因为部分资源在 HTTP 协议下可访问，但 HTTPS 下证书无效或存在内容差异。自动重定向会改变资源获取的实际行为，违背本项目的“原样归集”原则。用户如需切换协议，应手动编辑资源条目并注明变更原因。

**问：链接探测模块发现资源不可达时，系统会如何处理？**

答：探测引擎会连续发送三次 HEAD 请求（间隔 5 秒），若全部超时或返回 4xx/5xx 状态码，则在数据库中标记该资源为 “unreachable”，并记录最后一次响应状态码与探测时间。标记为不可达的资源仍然会保留在导航页中，但会附带警告标识。系统不会自动删除任何资源，所有处置决策需由管理员通过界面或脚本手动执行。

**问：批次号 86/130 的含义是什么？是否必须按顺序导入？**

答：批次号由主版本号与子版本号组成，86 表示当前迭代的主批次序列，130 表示该批次内包含的资源条目编号上限（非实际数量）。本项目不强制批次按数字顺序导入，但建议用户按照时间倒序或逻辑分组管理批次。导入脚本支持任意正整数作为批次标识，仅需保证同一批次号不重复覆盖即可。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:27
