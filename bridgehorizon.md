# Hyperlink Horizon

Hyperlink Horizon 是一个面向技术社区、科研工作者与信息聚合场景的轻量级外链资源导航与状态监控平台。项目定位为“技术外链的可靠基层设施”，核心目标用户包括开源项目维护者、技术文档编写者、社区运营人员以及需要长期追踪特定领域信息源的个人研究者。

本项目解决的核心问题是：大量有价值的技术资源链接分散在个人书签、浏览器收藏夹、内部文档与即时通讯记录中，缺乏集中化的展示、分类与可达性监控。Hyperlink Horizon 提供结构化的资源收录框架、可视化的状态指示面板以及标准化的外链输出格式，帮助用户将零散的 URL 转化为可维护、可共享、可审计的组织级知识资产。

## 功能概览

- **外链分类收纳**：支持按技术领域、文档类型、来源机构等多维度对链接进行树状分类，内置默认分类模板，减少从零搭建的重复劳动。

- **可达性即时检测**：对收录的每个 URL 执行定期 HTTP 状态检查，以可视化的颜色标记（通过/警告/失败）直观反映资源当前可访问状态，帮助快速定位失效链接。

- **标准输出模版**：提供符合开源项目 README 规范的资源列表导出功能，自动生成带 <code></code> 包裹的 URL 清单，满足文档编写与社区分享的格式要求。

- **批量导入与去重**：支持从文本文件、CSV 或浏览器书签导出文件批量导入链接，并基于 URL 标准化规则自动识别与合并重复条目。

- **状态历史记录**：记录每个 URL 在过去 30 天内的可达性变化趋势，以简易时间线形式呈现，便于评估外部资源的稳定性。

- **自定义标签系统**：允许用户为每个链接附加任意数量的自定义标签（如“#api”“#tutorial”“#official”），并支持基于标签组合的快速筛选。

- **只读只存原则**：项目明确不存储任何外链资源的副本或缓存内容，仅保存 URL 本身与元数据（分类、标签、状态记录），完全避免版权与内容合规风险。

## 应用场景

**开源项目文档站外链维护**  
开源项目维护者可将项目 README 或官网中引用的所有外部链接（如依赖库主页、API 参考、社区论坛）统一录入 Hyperlink Horizon，定期检查链接有效性，确保用户文档中的引用始终可访问。

**技术社区资源聚合页生成**  
技术社区运营人员可使用本项目的导出功能，快速生成格式规范的外链汇总页面，嵌入社区 Wiki 或月度资讯合集中，减少手动排版与格式校验时间。

**个人知识库外链审计**  
研究者或工程师在构建个人知识库（如基于 Markdown 或 Notion）时，可借助本系统对所引用的近百个外部链接进行集中登记与状态巡检，避免因链接失效导致笔记信息贬值。

**企业内部技术文档链接治理**  
企业技术文档团队可定期运行本系统，对公司内部 Confluence、GitLab Wiki 或 SharePoint 中散落的外部技术引用进行全量扫描与状态报告，作为文档健康度评估的辅助依据。

## 快速开始

以下步骤将在本地环境中完成 Hyperlink Horizon 的克隆、依赖安装与服务启动。

```bash
# 1. 克隆项目仓库
git clone https://github.com/hyperlink-horizon/hyperlink-horizon.git
cd hyperlink-horizon

# 2. 安装 Python 依赖（项目基于 Python 3.9+）
pip install -r requirements.txt

# 3. 初始化本地 SQLite 数据库（自动创建表结构）
python manage.py init-db

# 4. 导入示例链接数据（可选）
python manage.py load-fixtures

# 5. 启动开发服务器，默认监听 8000 端口
python manage.py run-server
```

启动成功后，在浏览器访问 `http://localhost:8000` 即可进入系统主界面。默认无账户体系，首次使用会提示创建本地管理员账户。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Python | 3.9, 3.10, 3.11 | 核心运行环境，不支持 3.8 以下版本 |
| SQLite | 3.35.0 及以上 | 内置数据库，用于存储链接元数据与状态历史 |
| requests | 2.28.0 及以上 | 用于执行 HTTP 可达性检测 |
| beautifulsoup4 | 4.11.0 及以上 | 可选，用于解析导入的 HTML 书签文件 |
| flask | 2.2.0 及以上 | Web 服务框架，提供 API 与界面 |
| click | 8.1.0 及以上 | 命令行交互工具集，用于管理命令 |
| pytest | 7.2.0 及以上 | 仅开发与测试时需要，生产环境可不安装 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `/docs/user-guide/` | 如何分类链接、如何查看状态报告、如何导出资源列表？ |
| 管理员指南 | `/docs/admin-guide/` | 如何配置检测频率、如何备份数据库、如何迁移服务器？ |
| API 参考 | `/docs/api-reference/` | 如何通过 HTTP API 批量添加或查询链接状态？ |
| 开发规范 | `/docs/development/` | 如何扩展新的导入格式、如何贡献代码、测试如何编写？ |

## 资源列表

本批次收录的外部资源链接共 7 条，均为组织外部分布式技术资讯站点或社区镜像。所有链接均以原始形式收录，未做任何协议或域名改写。

技术资讯与社区镜像

<code>https://yingchaobifenzhibob.org.cn</code>

<code>https://xijiabifenzhibob.org.cn</code>

<code>https://dejiabifenzhibob.org.cn</code>

<code>https://yijiabifenzhibob.org.cn</code>

<code>https://fajiabifenzhibob.org.cn</code>

<code>https://yingchaojishibifenc.org.cn</code>

<code>https://xijiajishibifenc.org.cn</code>

## 项目结构

```
hyperlink-horizon/                     # 项目根目录
├── app/                               # 主应用核心模块
│   ├── api/                           # RESTful API 路由与控制器
│   │   ├── routes.py                  # 路由注册与蓝图定义
│   │   └── validators.py              # 请求参数校验逻辑
│   ├── core/                          # 核心业务逻辑层
│   │   ├── checker.py                 # 可达性检测引擎（含重试与超时策略）
│   │   ├── importer.py                # 批量导入与去重处理器
│   │   └── exporter.py                # 标准格式导出生成器
│   ├── models/                        # 数据模型与 ORM 映射
│   │   ├── link.py                    # 链接实体模型（URL、分类、标签）
│   │   └── status.py                  # 状态历史记录模型
│   └── static/                        # 前端静态资源（CSS、JS）
│       └── dashboard.js               # 状态面板交互脚本
├── config/                            # 配置文件目录
│   ├── default.yaml                   # 默认配置（检测间隔、超时阈值）
│   └── production.yaml                # 生产环境覆盖配置
├── data/                              # 本地数据存储（SQLite 文件存放处）
│   └── horizon.db                     # 默认数据库文件（首次初始化生成）
├── docs/                              # 用户与开发文档
│   ├── user-guide/                    # 用户手册章节
│   └── api-reference/                 # API 详细参考文档
├── tests/                             # 单元测试与集成测试
│   ├── test_checker.py                # 检测引擎测试用例
│   └── test_importer.py               # 导入功能测试用例
├── manage.py                          # 命令行管理入口（init-db、run-server 等）
├── requirements.txt                   # Python 依赖清单（固定版本）
└── README.md                          # 项目概览文档（本文件）
```

## 贡献指南

1. 在 GitHub Issues 中查阅现有待办事项或提议新特性，获得社区讨论共识后再行开发，避免无效工作量。

2. 克隆项目并创建独立的功能分支（如 `feature/add-json-export`），确保分支名称清晰反映变更内容。

3. 遵循项目代码风格（PEP 8 基础，具体见 `.flake8` 配置文件），并为新增或修改的核心函数编写单元测试，测试覆盖率应不低于 85%。

4. 提交代码前运行完整测试套件（`pytest tests/`）并确保全部通过；同时使用 `python manage.py lint` 检查代码规范。

5. 发起 Pull Request 并填写标准模板，包含变更摘要、测试结果以及相关文档更新链接；PR 需要至少一位维护者审阅后方可合并。

## 常见问题

**问：系统检测到某个链接返回 403 状态码，但浏览器可以正常打开，是什么原因？**  
答：部分服务器会基于 `User-Agent` 或 `Accept` 请求头进行访问控制。Hyperlink Horizon 默认使用 `python-requests/2.28` 作为 User-Agent，这可能被目标服务器拒绝。您可以在配置文件中修改 `checker.user_agent` 字段，将其设为常见浏览器标识（如 Chrome 的 UA）以规避此类限制。同时，系统不支持携带 Cookie 或自定义认证头，涉及登录态的资源请勿纳入检测范围。

**问：导入大量链接（超过 1000 条）时页面响应变慢，如何优化？**  
答：批量导入操作在后台异步队列中执行，前端不阻塞。若仍需优化，建议将导入文件拆分为多个小于 500 条的文件分批提交。同时，项目支持配置 `importer.batch_size` 参数（默认 200），可调小该数值以降低单次事务压力。生产环境可考虑切换至 PostgreSQL 以获得更好的并发性能。

**问：导出的资源列表为何强制使用 <code></code> 包裹，且禁止转换为 Markdown 链接？**  
答：这是项目为保证外链原始性所确立的输出规范。使用 `<code>` 标签可在渲染时清晰区分 URL 与普通文本，避免自动解析为超链接导致的误触。同时，强制保留用户提供的原始格式（包括协议、大小写、结尾斜杠）是为了确保复制使用时与用户预期完全一致，避免因格式化修改引发的访问错误或校验失败。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:24
