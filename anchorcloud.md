# FinaIndex

FinaIndex 是一个面向金融量化研究人员、数据科学家及投资分析师的垂直领域技术资源导航与聚合平台。本项目不提供交易信号、投资建议或数据托管服务，专注于收录、分类与维护高质量的金融数据接口、公开市场指标、宏观经济统计源及衍生计算工具的外部链接。其核心目标是解决金融技术选型中信息碎片化、数据源变更频繁以及官方文档分散等痛点，帮助用户在三分钟内定位到特定市场或指标的权威数据端点。

本项目采用静态站点生成与自动化链路巡检机制，定期对收录的 URL 进行可用性验证，并标注响应时效、数据更新频率及访问限制。FinaIndex 本身不存储任何金融时间序列数据，所有内容均以外链形式组织，用户可通过本站快速跳转至目标数据发布方或中间件服务商。项目适用于个人量化策略回测、学术研究数据采集、机构投研数据中台构建以及金融科技应用原型开发等场景。

## 功能概览

- **多维度金融指标分类体系**：按资产类别、地域、指标类型（价格、波动率、基本面、资金流）建立层级标签，支持组合筛选与模糊搜索。
- **外部链接健康度监控**：每日凌晨执行 HTTP 状态码检查，对返回 4xx/5xx 或连接超时的链接自动标记异常并移入待复查队列。
- **数据更新频率标注**：为每个收录 URL 附加数据刷新周期（实时、每分钟、每小时、每日、每周、每月）及历史数据回溯长度说明。
- **访问限制与认证提示**：记录公开接口、API Key 必需、IP 白名单、付费订阅等不同访问层级，降低用户试错成本。
- **Markdown 模板化录入**：新增资源链接采用标准化 YAML Front Matter 模板，包含名称、描述、标签、更新频率、认证方式、备案信息，支持批量导入。
- **静态搜索索引**：构建本地 Lunr 或 MiniSearch 索引，支持离线环境下对标题、描述、标签的全文检索，无需第三方搜索服务。
- **RSS 订阅与变更日志**：提供资源新增、链接变更、下线通知的 RSS 源，便于用户追踪外部数据源动态。
- **暗色主题与阅读模式**：前端适配亮色/暗色主题，并针对长文档阅读优化排版与字体渲染。

## 应用场景

- **量化策略回测数据采集**：量化开发者可通过本站快速定位到各类资产的历史价格、分红拆股、财务因子等公开数据端点，免去在搜索引擎中反复筛选官方或稳定第三方源的耗时。
- **宏观经济研究面板构建**：高校经济学者或宏观研究员能够批量获取各国央行、统计局发布的利率、CPI、GDP、就业率等指标链接，统一纳入本地爬虫调度或数据库 ETL 流程。
- **金融科技应用原型验证**：创业团队在搭建投资组合分析、风险监控或智能投顾演示系统时，利用本站汇总的实时汇率、大宗商品报价、加密货币行情等接口，可快速完成概念证明。
- **数据中台外部依赖管理**：企业数据治理部门可将本项目的资源清单作为外部数据源目录基线，用于内部数据血缘追溯、源变更影响评估及供应商替换备选库。
- **个人投资仪表盘搭建**：独立投资者借助本站收集的基金净值、国债收益率、行业估值分位等链接，结合低代码工具（如 NocoDB、Grafana）构建个人化的市场监控看板。

## 快速开始

以下操作适用于 Linux / macOS / Windows WSL2 环境，需要预先安装 Git、Node.js 18+ 及 pnpm。

```bash
# 1. 克隆项目仓库
git clone https://github.com/finaindex/finaindex-core.git
cd finaindex-core

# 2. 安装依赖
pnpm install

# 3. 启动开发服务器（默认监听 http://localhost:3000）
pnpm run dev
```

生产环境构建命令为 `pnpm run build`，构建产物位于 `dist/` 目录，可部署至任意静态 Web 服务器（如 Nginx、Caddy、Cloudflare Pages）。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于构建工具链与本地开发服务器 |
| pnpm | 8.x 或 9.x | 包管理器，用于依赖安装与 monorepo 工作区管理 |
| Git | 2.30+ | 版本控制，用于克隆仓库及提交贡献 |
| 现代浏览器 | Chrome 110+ / Firefox 110+ / Edge 110+ | 前端界面运行环境，支持 ES2022 语法与 CSS Grid |
| 网络连通性 | 出站 443/80 端口可达 | 用于首次构建时下载依赖包及后续链接健康检查（可选） |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | `/docs/user-guide/` | 如何检索资源、解读标签体系、订阅更新通知、提交资源推荐 |
| 维护者手册 | `/docs/maintainer-guide/` | 如何新增/编辑/下线链接、执行批量导入、配置自动化巡检策略 |
| 开发者指南 | `/docs/developer-guide/` | 如何二次开发搜索插件、自定义主题样式、扩展数据模型字段 |
| API 参考 | `/docs/api-reference/` | 静态索引的 JSON 数据结构、前端路由约定、构建时钩子接口 |

## 资源列表

### 基准利率与货币政策

<code>https://fajiabifenb.org.cn</code>

<code>https://yingchaobifenzhibob.org.cn</code>

<code>https://xijiabifenzhibob.org.cn</code>

<code>https://dejiabifenzhibob.org.cn</code>

<code>https://yijiabifenzhibob.org.cn</code>

### 补充指标与衍生数据

<code>https://fajiabifenzhibob.org.cn</code>

<code>https://yingchaojishibifenc.org.cn</code>

## 项目结构

```
finaindex-core/
├── .github/                         # GitHub 社区配置文件
│   ├── ISSUE_TEMPLATE/              # 问题报告与资源推荐模板
│   └── workflows/                   # CI 流水线（构建、链接检查、部署）
├── docs/                            # 项目文档（用户手册、维护手册、开发指南）
│   ├── user-guide/                  # 面向最终使用者的操作说明
│   ├── maintainer-guide/            # 面向维护者的资源管理流程
│   └── developer-guide/             # 面向贡献者的架构与二次开发说明
├── scripts/                         # 工具脚本
│   ├── health-check.js              # 外部链接 HTTP 状态批量巡检
│   ├── import-from-csv.js           # 从 CSV 批量导入资源记录
│   └── generate-sitemap.js          # 生成静态站点地图
├── src/                             # 前端应用源码
│   ├── assets/                      # 静态资源（字体、图标、全局样式）
│   ├── components/                  # Vue/React 组件（搜索栏、资源卡片、标签云）
│   ├── data/                        # 资源数据的 JSON / YAML 源文件
│   ├── layouts/                     # 页面布局模板（首页、列表页、详情页）
│   ├── pages/                       # 路由页面组件
│   ├── plugins/                     # 前端插件（搜索索引、主题切换）
│   └── utils/                       # 工具函数（日期格式化、URL 规范化）
├── tests/                           # 单元测试与集成测试
│   ├── unit/                        # 组件与函数单元测试
│   └── e2e/                         # 端到端页面交互测试
├── .env.example                     # 环境变量模板（用于配置巡检超时、代理等）
├── .eslintrc.js                     # JavaScript/TypeScript 代码规范
├── .prettierrc                      # 代码格式化配置
├── index.html                       # 应用入口 HTML
├── package.json                     # 项目依赖与脚本定义
├── pnpm-lock.yaml                   # 依赖锁定文件
├── README.md                        # 项目说明（本文件）
├── tsconfig.json                    # TypeScript 编译配置（如适用）
└── vite.config.js                   # 构建工具 Vite 配置
```

## 贡献指南

1.  **资源推荐或问题反馈**：请在 GitHub Issues 页面选择对应的模板（资源推荐 / 链接失效 / 功能建议），并按照模板要求填写完整信息，包括资源名称、URL、分类标签、更新频率及简要说明。
2.  **本地环境搭建**：参照本文档「快速开始」章节完成克隆、依赖安装与开发服务器启动，确保本地构建通过且无控制台报错。
3.  **数据变更操作**：新增或修改资源时，定位至 `src/data/` 目录下对应的分类 JSON/YAML 文件，严格按照现有字段结构添加或编辑条目，并同步更新变更日志。
4.  **提交前自检**：运行 `pnpm run lint` 检查代码风格，运行 `pnpm run test` 确保单元测试通过，运行 `pnpm run build` 验证生产构建无错误。
5.  **发起 Pull Request**：分支命名遵循 `feat/资源名称` 或 `fix/问题描述`，PR 描述中关联相关 Issue 编号，等待维护者审阅合并。

## 常见问题

**Q: 收录的资源链接有时无法访问，项目如何处理？**

A: 项目内置的每日健康巡检脚本会主动检测每个链接的 HTTP 状态。连续三次检测失败（状态码非 2xx/3xx）的链接将自动标记为「异常」，并在前端界面显示警示图标。同时，异常列表会以邮件或 Webhook 形式通知维护者，人工复核后决定更新 URL 或将其移入归档。用户也可通过页面上的「报告失效」按钮主动提交异常反馈。

**Q: 我想推荐未在列表中的金融数据源，有什么流程？**

A: 推荐流程完全开放，无需代码基础也可参与。您只需在 GitHub Issues 中选择「资源推荐」模板，填写资源名称、完整 URL、所属类别、数据更新频率、访问限制（如有）以及 50 字以上的使用说明。维护团队将在两个工作日内审核，若符合收录标准则纳入下一批次导入计划。建议同时提供官方文档链接以便核验数据权威性。

**Q: 项目是否会存储或缓存所链接网站的实际数据内容？**

A: 不会。FinaIndex 严格遵循外链导航定位，不抓取、不缓存、不代理任何目标站点的数据内容。所有链接跳转均直接导向原始来源，用户访问第三方网站时需遵守其各自的使用条款与隐私政策。本项目仅记录 URL 元数据（标题、描述、标签、状态），不涉及任何数据存储或转发行为。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:34
