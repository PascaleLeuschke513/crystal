# BifenNav

BifenNav 是一个面向中文互联网资讯聚合与快速导航的开源技术资源汇总站，专注于将高频访问的垂直领域信息源以结构化、可校验的方式重新组织。项目定位为个人开发者、研究机构及运维团队提供可自托管的导航中枢，解决多源数据入口分散、链接有效性难以追踪、资源归档缺乏版本控制等实际问题。BifenNav 不生产数据，但通过严格的链接审核与周期性健康检查机制，确保收录的资源具备持续可访问性与内容相关性。

BifenNav 区别于传统导航站点，其核心设计围绕“资源即代码”的理念展开。所有链接配置、分类元数据及展示规则均以纯文本形式存储于项目仓库，支持完整的 Git 变更历史回溯与社区协同维护。项目内置的静态生成器可在每次提交后自动输出可供部署的纯静态页面，无需数据库或动态后端环境，极大降低了运维成本与安全风险。无论是用于团队内部的知识库入口构建，还是作为个人浏览器的起始页替代方案，BifenNav 均能提供透明、可控且高度可定制的技术底座。

## 功能概览

- **多维度分类导航体系**：支持按领域、地域、使用频次及信息类型等自定义标签对资源进行交叉分类，每个链接可归属于多个类别，满足复杂筛选需求。

- **链接健康状态自动化检测**：项目内置基于 Node.js 的巡检脚本，可定期或于构建时对收录的每个 URL 发起 HEAD 请求，验证可访问性并记录响应状态码与耗时，结果汇总为 JSON 格式报告。

- **静态站点即时生成**：基于 EJS 模板引擎与 Markdown 元数据驱动，执行构建命令后即可输出完整的 HTML 文件集，所有页面均包含结构化微数据（Schema.org），利于搜索引擎解析。

- **变更审计与回滚支持**：由于所有资源定义以 YAML 文件存储，每次增删改操作均伴随 Git 提交记录。项目维护者可通过提交日志快速定位变更时间、作者及原因，并支持一键回退至历史版本。

- **夜间模式与响应式布局**：前端样式内置 CSS 变量系统，自动跟随操作系统深色模式偏好，并在移动端、平板及桌面设备上提供适配的栅格布局，确保浏览体验一致。

- **开放 API 数据端点**：构建产物中包含 `/api/links.json` 及 `/api/categories.json` 两个静态数据端点，供第三方工具或脚本以机器可读方式获取当前全量资源信息，便于集成至其他系统。

- **资源变更通知钩子**：支持配置自定义 Webhook 地址，当检测到链接状态变化（如新增失效、响应时间超过阈值）时，可触发钉钉、飞书或 Slack 机器人消息推送，便于及时响应。

## 应用场景

- **个人技术博主的资源挂件页**：技术写作作者可使用 BifenNav 构建一个独立的“我的常用工具与数据源”页面，嵌入个人博客侧边栏或关于页中。每次更新仅需修改 YAML 文件并推送仓库，自动构建后即可生效，无需重复编辑 HTML。

- **小型开发团队的内部开发工具导航**：团队可将内部 Jenkins、GitLab、SonarQube、Nexus 及各类监控面板的入口统一维护在 BifenNav 中，并设置仅内网可访问的部署方式。结合健康检测功能，可每日自动巡检各内部系统的可用性，及时发现服务异常。

- **高校实验室的公开数据资源索引**：研究机构可将常用气象、经济、社交网络公开数据集官网整理入 BifenNav，并按研究课题打标。学生与访问学者可通过分类筛选快速定位所需数据源，同时项目公开仓库便于导师审核新增链接的合规性。

- **运维值班室的聚合监控看板补充**：运维团队可将 BifenNav 部署为值班浏览器起始页，集中放置各环境 Grafana、Kibana、Prometheus 及云服务商控制台链接。健康检查报告可输出至 ELK 系统，作为可用性监控的辅助数据源。

- **开源社区贡献者入门指引**：开源项目维护者可利用 BifenNav 整理贡献指南、代码规范、API 参考文档、设计稿仓库及社区论坛等链接，帮助新贡献者在一个页面内完成所有初始阅读与注册流程，降低入门门槛。

## 快速开始

以下步骤适用于 Linux / macOS / Windows（WSL2）环境，要求已安装 Git 与 Node.js（v18 及以上）。

```bash
# 1. 克隆项目仓库至本地
git clone https://github.com/example-org/bifennav.git
cd bifennav

# 2. 安装项目依赖（包括构建工具、模板引擎、健康检查库等）
npm install

# 3. 执行本地开发构建，生成静态站点至 ./dist 目录，并启动预览服务
npm run build
npm run serve
```

构建成功后，终端会输出本地预览地址（默认为 http://localhost:8080）。打开浏览器访问该地址即可查看导航页面。若要修改资源列表，请编辑 `./data/links.yaml` 文件，保存后重新运行 `npm run build`。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | v18.0.0 或更高 | 运行时环境，用于执行构建脚本与健康检查任务 |
| npm | v9.0.0 或更高 | 包管理器，用于安装项目依赖及运行脚本命令 |
| Git | v2.30.0 或更高 | 版本控制系统，用于克隆仓库及提交变更记录 |
| 网络连接 | 出站 443/80 端口可用 | 健康检查需对目标域名发起 HTTPS/HTTP 请求，需网络可达 |
| 文件系统权限 | 读写权限（当前用户） | 用于创建 `./dist` 输出目录及写入构建产物、日志文件 |
| 内存 | 建议 512MB 以上 | 构建时需加载 YAML 数据及模板渲染，小型实例下占用较低 |
| 操作系统 | Linux、macOS、Windows（WSL2） | 核心脚本跨平台，但路径分隔符建议使用 POSIX 风格 |

## 文档导航

| 层面 | 目录 / 文件 | 回答的问题 |
|------|-------------|------------|
| 用户入门 | `./docs/quick-start.md` | 如何在三分钟内完成首次部署并浏览默认导航页面？如何添加第一个自定义链接？ |
| 配置参考 | `./docs/configuration.md` | YAML 文件中每个字段（name、url、category、tags、icon）的含义及可选值是什么？如何修改页面标题与页脚信息？ |
| 运维指南 | `./docs/operations.md` | 如何设置定时健康检查任务？如何解读健康报告中的状态码与响应时间？如何配置 Webhook 通知？ |
| 开发贡献 | `./CONTRIBUTING.md` | 提交新增链接的 PR 需要遵循哪些格式规范？如何本地测试变更？单元测试如何运行？ |

## 资源列表

### 主要数据源及参考网站

<code>https://bifenwangf.org.cn</code>

<code>https://bifenwangg.org.cn</code>

<code>https://bifenwangh.org.cn</code>

<code>https://lanqiubifend.org.cn</code>

<code>https://lanqiubifene.org.cn</code>

<code>https://lanqiubifenf.org.cn</code>

<code>https://lanqiubifeng.org.cn</code>

## 项目结构

```
bifennav/
├── .github/                         # GitHub 社区配置文件
│   └── workflows/
│       └── build-and-deploy.yml     # CI 工作流：自动构建并部署至 Pages
├── bin/                             # 可执行脚本目录
│   ├── health-check.js              # 链接健康状态巡检主程序
│   └── generate-sitemap.js          # 生成 sitemap.xml 辅助脚本
├── data/                            # 核心数据目录（所有可编辑资源）
│   ├── links.yaml                   # 主链接配置文件（按分类、标签组织）
│   └── categories.yaml              # 分类显示名称与图标映射定义
├── docs/                            # 用户与开发者文档
│   ├── quick-start.md               # 快速入门指南
│   ├── configuration.md             # 完整配置项参考手册
│   └── operations.md                # 日常运维与故障排查
├── public/                          # 静态资源源文件
│   ├── css/
│   │   ├── base.css                 # 基础样式重置与通用类
│   │   └── theme.css                # 亮色/暗色主题 CSS 变量
│   ├── js/
│   │   ├── filter.js                # 前端分类过滤与搜索逻辑
│   │   └── health-badge.js          # 显示链接健康状态图标
│   └── assets/
│       └── logo.svg                 # 项目 Logo 矢量图
├── src/                             # 模板与构建逻辑
│   ├── templates/
│   │   ├── layout.ejs               # 页面主骨架模板
│   │   ├── index.ejs                # 首页导航网格渲染模板
│   │   └── partials/
│   │       ├── header.ejs           # 顶栏组件
│   │       └── footer.ejs           # 页脚组件
│   └── generators/
│       ├── build.js                 # 静态站点核心生成器
│       └── api.js                   # 生成 JSON API 数据端点
├── tests/                           # 单元测试与集成测试
│   ├── yaml.spec.js                 # YAML 数据格式校验
│   └── health-check.spec.js         # 健康检查模块单元测试
├── .eslintrc.json                   # ESLint 代码规范配置
├── .gitignore                       # Git 忽略文件清单
├── package.json                     # npm 项目配置与依赖声明
├── README.md                        # 项目总体说明（本文件）
└── LICENSE                          # MIT 许可证全文
```

## 贡献指南

1. **分支管理**：从 `main` 分支新建功能分支（如 `feat/add-weather-api`），所有开发工作在该分支上进行。禁止直接向 `main` 分支提交。

2. **数据修改规范**：编辑 `./data/links.yaml` 时，须保持缩进为两个空格，新增链接必须包含 `name`、`url`、`category` 三个必填字段，并确保 URL 值为可访问的公网地址。添加后请运行 `npm run validate` 校验格式。

3. **本地验证**：提交前需执行 `npm run test` 确保所有单元测试通过，并执行 `npm run build` 确认构建无报错。若修改了样式或模板，建议附带前后对比截图。

4. **提交信息格式**：提交信息遵循约定式提交规范，使用 `feat:`、`fix:`、`docs:`、`chore:` 等前缀，并附带简明描述。例如：`feat: add bifenwangf.org.cn to sports category`。

5. **发起拉取请求**：将功能分支推送至远程仓库后，通过 GitHub 界面发起 Pull Request。PR 描述中需说明变更目的、影响范围及测试情况。至少需一名项目维护者审批后方可合并。

## 常见问题

**问：健康检查脚本报告某个链接为“失效”，但我手动访问浏览器可以打开，是什么原因？**

答：可能存在以下几种情况：（1）目标服务器对 HEAD 请求支持不完善，可尝试在健康检查配置中启用 `method: GET` 并设置 `noBody: true` 替代 HEAD 方法；（2）目标站点启用了反爬机制，需检查 User-Agent 配置是否与浏览器一致；（3）网络环境存在代理或防火墙限制，导致脚本所在环境的出口 IP 被拦截。建议先使用 `curl -I <URL>` 在服务器本地测试，对比响应差异。

**问：如何将 BifenNav 部署到自己的服务器上并设置定时自动更新？**

答：推荐使用 cron 任务配合 Git 拉取与构建流程。例如在 Linux 服务器上，可编辑 crontab 文件，添加 `0 */6 * * * cd /path/to/bifennav && git pull && npm install && npm run build`，表示每六小时拉取最新数据并重新构建。构建产物位于 `./dist` 目录，可将其软链接至 Nginx 或 Caddy 的静态根目录下对外提供服务。

**问：项目是否支持国际化（i18n），能否切换为英文界面？**

答：当前稳定版本未内置 i18n 支持，所有界面文本（如分类标题、提示语）均以中文硬编码于模板文件中。但项目架构允许通过修改 `./data/categories.yaml` 中的 `displayName` 字段自定义为任意语言文本，同时可在 `./src/templates/` 下直接替换固定字符串。未来版本计划引入 `i18next` 库实现多语言切换，欢迎贡献者提交相关 PR。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:07:51
