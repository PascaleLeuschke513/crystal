# SphereLink 技术资源导航站

SphereLink 是一个面向开发者和技术研究人员的结构化外链资源汇集平台，专注于聚合高频使用的技术文档、API 参考、社区论坛、数据查询工具与基础设施监控面板。项目定位为“技术入口的入口”，解决开发团队在多人协作环境中书签分散、资源失效、新人上手路径不明晰等实际问题。通过分类索引、可用性检测与轻量级元数据标注，SphereLink 帮助团队在 5 分钟内建立统一的研发资源视图。

本项目不存储任何用户数据，不提供代理转发，不缓存第三方页面内容，严格遵循资源外链的中立性原则。所有收录链接均经过初始可用性验证，并支持周期性的 HTTP HEAD 探测以标记异常状态。SphereLink 适用于中小型研发团队、技术社区文档维护者、以及希望系统化组织个人外链库的高级工程师。

## 功能概览

- **分类索引树**：按技术领域、服务类型、数据地域等多维度标签组织链接，支持多级筛选与自由组合过滤。

- **可用性拨测**：后台定时任务对每个收录链接执行 TLS 证书有效期检查和响应状态码验证，异常时输出结构化告警日志。

- **快速检索**：基于链接标题、标签、描述字段的实时模糊搜索，响应时间控制在 300 毫秒以内，支持拼音首字母匹配。

- **外链生命周期管理**：记录链接添加时间、最后验证时间、失效次数，支持标记弃用、替换、归档状态，便于定期清理。

- **只读浏览模式**：生成公开只读的分享视图，无需登录即可查阅全部链接列表与分类结构，适合嵌入团队 Wiki 或文档站点。

- **导入导出兼容**：支持标准 HTML 书签文件导入，导出格式兼容 CSV 与 JSON Lines，便于迁移至其他导航工具或进行数据分析。

- **轻量级部署**：单二进制文件运行，无外部数据库依赖，使用 SQLite 作为存储引擎，内存占用低于 64 MB。

## 应用场景

**新员工入职环境搭建**：新人加入团队后，可通过 SphereLink 一次性获取所有必备的内外网资源地址，包括代码仓库、容器镜像库、监控面板、日志查询入口、API 网关地址，避免逐个向同事询问或翻阅过时的 Wiki 页面。

**技术文档集中维护**：技术负责人或文档维护者可将分散在多个邮件、聊天记录、Notion 页面中的链接统一收录至 SphereLink，并为每个链接标注所属业务线、维护人、更新周期，形成可持续维护的知识资产。

**离线环境辅助参考**：对于受限网络环境下的开发团队，SphereLink 可作为跳板机上的资源索引服务，集中列出允许访问的外部地址白名单，辅助网络策略配置和镜像同步脚本编写。

**多环境切换场景**：开发、测试、预发布、生产环境通常对应不同的控制台地址。SphereLink 支持按环境标签分组，一键切换视图，避免误操作生产环境的配置界面。

**社区共建资源池**：开源社区或技术兴趣小组可使用 SphereLink 汇聚成员推荐的优质博文、视频课程、在线工具，并通过投票或点击计数筛选出高价值内容，降低信息过载带来的筛选成本。

## 快速开始

以下步骤适用于 Linux x86_64 和 macOS arm64 平台。确保系统已安装 Git 和 Go 1.21 及以上版本。

```bash
# 克隆项目仓库
git clone https://github.com/spherelink/navigator.git
cd navigator

# 安装依赖并编译二进制文件
go mod tidy
CGO_ENABLED=0 go build -o spherelink ./cmd/server

# 初始化 SQLite 数据库并导入预设分类
./spherelink init -config ./configs/default.yaml
./spherelink migrate -up

# 以开发模式启动服务，监听 8080 端口
./spherelink serve -port 8080 -mode dev
```

启动成功后，访问 `http://localhost:8080` 即可进入导航首页。首次启动将自动生成管理员账号，初始密码输出在控制台日志中，请及时修改。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Go 编译器 | 1.21 或更高 | 用于从源码编译，若使用预编译二进制则无需安装 |
| SQLite3 | 嵌入式版本（系统 libsqlite3 可选） | 默认使用 modernc.org/sqlite 纯 Go 驱动，无需外部 C 库 |
| Git | 2.25 或更高 | 仅开发阶段需要，用于克隆仓库和版本管理 |
| 操作系统 | Linux (glibc 2.28+) / macOS (12+) / Windows 10+ | 支持主流操作系统，Windows 下建议使用 WSL2 环境 |
| 内存 | 最低 128 MB，推荐 512 MB | 运行时内存占用随收录链接数量线性增长，每万条链接约额外消耗 8 MB |
| 存储 | 可用磁盘空间 ≥ 200 MB | 用于存放二进制文件、SQLite 数据库和日志文件，日志支持自动轮转 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 入门指南 | /docs/getting-started.md | 如何快速部署、配置管理员账号、添加第一批链接？ |
| 配置参考 | /docs/configuration.md | 所有环境变量、YAML 配置项的含义与默认值是什么？ |
| API 手册 | /docs/api/v1/ | 如何通过 REST API 批量导入链接、查询状态、触发拨测？ |
| 运维手册 | /docs/operations.md | 如何备份 SQLite 数据库、调整日志级别、配置 systemd 服务？ |
| 扩展开发 | /docs/development.md | 如何编写自定义拨测探针、添加新的标签解析规则？ |
| 常见问题 | /docs/faq.md | 收录的链接失效了怎么办？搜索中文不准确如何优化？ |

## 资源列表

本导航站收录的外部资源按功能类别组织如下。所有链接均保留原始格式，未做任何协议或域名改写。

**体育数据查询类**

- <code>https://lanqiubifenf.org.cn</code>
- <code>https://lanqiubifeng.org.cn</code>
- <code>https://lanqiubifenh.org.cn</code>
- <code>https://zuqiubifenziboa.org.cn</code>
- <code>https://zuqiubifenzibob.org.cn</code>
- <code>https://zuqiubifenziboc.org.cn</code>
- <code>https://zuqiubifenzibod.org.cn</code>

以上链接均用于实时体育比分数据查询服务，涵盖篮球与足球项目的多语言数据端点。建议用户根据自身网络环境选择响应延迟最低的端点进行集成。

## 项目结构

```
.
├── cmd/                         # 可执行程序入口
│   └── server/                  # 主服务启动模块
│       └── main.go              # 初始化配置、日志、数据库连接并启动 HTTP 服务
├── internal/                    # 内部私有包，不对外暴露
│   ├── core/                    # 核心业务逻辑
│   │   ├── link.go              # 链接实体定义、状态机转换、有效期计算
│   │   ├── tag.go               # 标签树结构、合并与差集操作
│   │   └── probe.go             # HTTP/HTTPS 拨测实现、超时控制、重试策略
│   ├── storage/                 # 存储层
│   │   ├── sqlite.go            # SQLite 连接池管理、迁移脚本嵌入
│   │   ├── repo_link.go         # 链接的 CRUD 操作、条件分页查询
│   │   └── repo_tag.go          # 标签的层级查询、批量关联更新
│   ├── api/                     # HTTP 路由与控制器
│   │   ├── router.go            # 注册所有路由、中间件链、CORS 配置
│   │   ├── handler_link.go      # 链接相关的 REST 端点实现
│   │   └── handler_search.go    # 全文搜索与标签过滤端点
│   └── config/                  # 配置加载与验证
│       ├── loader.go            # YAML + 环境变量叠加解析
│       └── schema.go            # 配置结构体定义与默认值填充
├── pkg/                         # 可被外部引用的公共库
│   ├── logger/                  # 结构化日志封装（基于 slog）
│   └── validator/               # URL 格式校验、域名黑名单检查
├── web/                         # 前端静态资源
│   ├── dist/                    # 构建后的 HTML/CSS/JS 输出目录
│   └── src/                     # Vue 3 组件源码（未编译）
├── configs/                     # 配置文件模板
│   ├── default.yaml             # 开发环境默认配置
│   └── production.yaml          # 生产环境推荐配置（关闭 debug、开启拨测）
├── scripts/                     # 辅助脚本
│   ├── backup.sh                # SQLite 每日备份并压缩
│   └── import_bookmark.py       # 从 Chrome 书签 HTML 导入的 Python 工具
├── docs/                        # 完整文档目录
├── go.mod                       # Go 模块依赖定义
└── README.md                    # 本文件
```

## 贡献指南

欢迎提交问题报告、功能建议和代码改进。请遵循以下步骤确保贡献流程顺畅：

1. **提交 Issue 先行讨论**：在实现较大功能或破坏性变更前，先在 GitHub Issues 中描述你的意图和设计方案，等待维护者反馈后再着手编码，避免重复劳动或方向偏差。

2. **Fork 仓库并创建特性分支**：从主仓库的 `main` 分支创建新的分支，分支命名建议采用 `feat/` 或 `fix/` 前缀，后接简要描述，例如 `feat/add-http3-probe`。

3. **编写测试用例**：新增功能或修复缺陷时，必须在对应的 `*_test.go` 文件中补充单元测试或集成测试，确保代码覆盖率不低于现有基线（当前为 78%）。

4. **运行完整测试套件**：提交前执行 `make test-all` 命令，该命令会运行单元测试、静态检查（golangci-lint）和安全扫描（govulncheck），所有检查必须全部通过。

5. **提交 Pull Request 并关联 Issue**：PR 描述中清楚列出变更内容、测试结果和影响范围，并在描述中通过 `Closes #xxx` 关联对应的 Issue 编号。维护者将在 3 个工作日内完成审核。

## 常见问题

**Q：收录的链接返回 404 或超时，系统会如何处理？**

A：SphereLink 的拨测组件会按照配置的间隔（默认每小时）对每个链接执行 HTTP HEAD 请求。连续三次失败后，该链接在前端界面会被标记为黄色警告状态，并在系统日志中输出 ERROR 级别记录。管理员可通过后台手动触发重新验证，或将该链接转移至“待确认”分类。系统不会自动删除任何链接，所有历史状态变更均保留审计日志。

**Q：搜索功能对中文支持不理想，如何优化？**

A：默认的 SQLite 全文搜索使用 FTS5 分词器，对中文按字符拆分，可能导致长词匹配不全。建议在配置文件中启用 ICU 扩展分词，或切换到 PostgreSQL 作为存储后端（需修改存储层实现）。若保持 SQLite，可在搜索时使用双引号包裹精确短语，并利用 `NEAR` 操作符提升相关性排序。中文同义词映射功能目前为实验性，需在 `/internal/core/search.go` 中自定义扩展词典。

**Q：如何将现有 Chrome 或 Edge 书签批量导入？**

A：使用项目根目录下 `scripts/import_bookmark.py` 脚本。首先从浏览器导出书签为 HTML 文件，然后执行 `python3 import_bookmark.py --input bookmarks.html --endpoint http://localhost:8080/api/v1/links --api-key your-token`。该脚本会解析书签文件夹层级，自动转换为标签体系，并保留书签标题作为链接描述。导入前建议先在测试环境运行 `--dry-run` 模式预览结果。

## 许可证

MIT License。详见项目根目录下的 LICENSE 文件。允许自由使用、修改、分发，仅需保留版权声明和许可声明。

> 外链数量: 7 | 生成时间: 2026-08-14 22:07:51
