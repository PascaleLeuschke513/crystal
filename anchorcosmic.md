# PortalX

PortalX 是一个面向技术内容创作者与开源社区维护者的轻量化外链聚合与导航系统。该项目旨在解决个人或小型团队在运营多个技术博客、文档站点、视频频道及社交账号时面临的链接分散、资源入口不统一的问题，通过提供一个集中式的资源导航门户，帮助内容发布者提升其受众的访问效率与信息获取体验。PortalX 并非一个通用的书签管理工具，而是专为需要定期发布技术教程、直播回放、系列文章及配套源码的开源教育工作者设计，通过结构化的分类与清晰的视觉引导，将观众或读者从单一入口精准导向各类深度资源。

## 功能概览

- **多级分类导航体系**：支持管理员自定义无限层级的资源分类目录，便于按技术领域、内容形式或项目阶段组织链接，每个分类均可独立设置图标与描述文案。
- **外链健康状态监测**：系统每日定时检测已收录链接的可访问性，自动标记响应超时或返回 HTTP 错误状态码的条目，并在管理后台高亮提醒，帮助维护者及时发现失效资源。
- **访问统计与点击分析**：为每个外链记录点击次数、最近点击时间及来源页面，提供基于时间维度的热度排序功能，使维护者能够清晰了解受众的关注重点与内容偏好变化。
- **一键导入与批量编辑**：支持通过 CSV 文件或标准 OPML 格式批量导入现有书签集合，同时提供按域名、分类或关键词筛选后的批量启用、停用及删除操作，大幅提升资源库的初始化与日常维护效率。
- **响应式前端模板**：内置两套适配移动端与桌面端的导航主题，一套侧重列表式信息密度，另一套采用卡片式视觉布局，均严格遵循 WCAG 2.1 AA 级无障碍标准，确保不同设备与访问条件下的基础可用性。
- **开放数据 API 接口**：提供基于 RESTful 风格的只读 JSON API，允许第三方工具或脚本程序直接获取分类结构及链接列表，方便与其他自动化工作流（如文档站点生成器或 CI/CD 流水线）集成。
- **资源收藏与个人面板**：为注册用户提供个人收藏夹功能，支持将常用链接标记为“常用”并形成独立的快捷访问列表，同时保留用户最近访问的 20 条历史记录。

## 应用场景

- **技术系列课程配套导航**：当开源教育者发布一套包含多章节视频教程、配套示例代码仓库、在线演示环境和课后习题文档的完整课程时，可使用 PortalX 创建专属导航页，将所有相关资源按学习阶段归类，学员仅需记住一个入口地址即可直达所有学习材料。
- **开源项目多站点入口统一**：一个中型开源项目通常拥有项目官网、开发文档、社区论坛、版本发布说明、实时演示站及贡献者行为准则等多个独立子站点。项目维护团队可利用 PortalX 建立官方资源导航页，替代分散在各处的 README 链接列表，为社区贡献者与新用户提供结构清晰的入口指引。
- **技术直播活动资源聚合**：组织线上技术峰会或系列直播分享时，运营团队可将每期直播回放链接、讲师幻灯片下载地址、在线提问收集表以及相关代码仓库集中收录于 PortalX 的一个分类节点下，方便参会者在活动结束后快速回顾与深入探索。
- **开发者个人品牌资源站**：独立技术博主、开源贡献者或行业顾问可以将自己撰写的专栏文章、公开演讲录像、发布的 npm 包、GitHub 组织及技术交流社群入口汇聚于个人 PortalX 实例，作为个人技术品牌的统一流量收口，简化受众寻找其产出内容的路径。

## 快速开始

以下指令适用于 Linux 或 macOS 环境，请确保系统已安装 Git、Node.js（v18.0 或更高版本）及 npm（v9.0 或更高版本）。

```bash
# 步骤 1：克隆项目仓库至本地
git clone https://github.com/portalx-dev/portalx-core.git
cd portalx-core

# 步骤 2：安装项目依赖
npm install

# 步骤 3：以开发模式启动本地服务
npm run dev
```

执行上述命令后，终端将输出本地访问地址（通常为 http://localhost:5173），打开浏览器访问该地址即可预览导航首页。生产环境部署请参考文档导航章节中的部署指南。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 18.x 或 20.x LTS | 项目运行时与构建工具链的基础环境，低于 18 版本将无法支持 ES Module 加载机制 |
| npm | 9.x 或更高 | 用于安装项目依赖包及执行脚本命令，建议与 Node.js LTS 版本捆绑升级 |
| SQLite | 3.35 或更高 | 默认内嵌数据库引擎，用于存储分类、链接及统计信息，无需额外安装服务进程 |
| Git | 2.25 或更高 | 版本控制工具，用于克隆仓库及后续拉取更新，部分 CI 流程依赖 Git 标签识别版本号 |
| 现代浏览器 | 最近两个主要版本 | 前端界面基于 Chromium 与 Firefox 渲染引擎特性构建，不兼容 Internet Explorer 及旧版 Edge |
| 服务器操作系统 | Linux (Ubuntu 20.04+) / macOS 12+ | 生产部署推荐使用 Linux 环境，Windows 系统可通过 WSL2 获得兼容运行能力 |
| 内存 | 最低 512 MB，推荐 1 GB | 运行时内存占用主要包括 SQLite 缓存与 Node.js 进程，高并发场景需按实际负载扩容 |
| 磁盘空间 | 最低 200 MB | 用于存放代码文件、SQLite 数据库及日志，日志轮转策略默认保留最近 30 天记录 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 入门指南 | /docs/getting-started.md | 如何从零开始初始化 PortalX 实例，包括首次启动、管理员账号创建及基础分类设置 |
| 部署手册 | /docs/deployment/ | 如何将开发完成的导航站点部署至生产服务器，涵盖 Nginx 反向代理配置、SSL 证书绑定及 systemd 服务守护 |
| 管理后台操作 | /docs/admin-panel.md | 管理员后台每个功能模块的具体操作步骤，包括链接增删改、分类调整、数据导入导出及健康检查日志查看 |
| API 参考 | /docs/api-reference.md | 对外提供的 REST API 端点列表、请求参数格式、响应数据结构及认证方式说明，供第三方集成开发者查阅 |
| 主题定制指南 | /docs/theming.md | 如何修改前端界面样式、自定义布局模板或开发全新主题，包含 SCSS 变量表与组件覆写规则 |
| 数据备份与恢复 | /docs/backup.md | 如何通过命令行工具或定时任务备份 SQLite 数据库文件，以及从备份文件恢复数据的详细流程 |

## 资源列表

以下链接为本项目收录的部分外部资源，按类别整理如下。所有链接均保持原始格式原样列出。

技术直播与视频回放类

<code>https://rihanzhibofulishipin.org.cn</code>

<code>https://rewuzhibowanghongzhibo.org.cn</code>

<code>https://wanghongmeinvrewuzhibo.org.cn</code>

<code>https://wufuyewanghongzhibo.org.cn</code>

<code>https://wufuyemeinvzhibo.org.cn</code>

<code>https://meinvwufuyiezhibo.org.cn</code>

<code>https://shuaigefujifulizhibo.org.cn</code>

## 项目结构

```
portalx-core/
├── src/                                 # 源代码主目录
│   ├── api/                            # REST API 路由与控制器实现
│   │   ├── v1/                         # API 版本 v1 端点定义
│   │   │   ├── categories.js           # 分类资源 CRUD 操作接口
│   │   │   ├── links.js                # 链接资源查询与统计接口
│   │   │   └── health.js               # 链接健康状态批量检查接口
│   │   └── middleware/                 # 认证、日志与错误处理中间件
│   ├── core/                           # 业务逻辑核心层
│   │   ├── database/                   # SQLite 数据库连接与查询构建器
│   │   │   ├── client.js               # 数据库客户端初始化与连接池管理
│   │   │   └── migrations/             # 数据库表结构版本迁移脚本
│   │   ├── crawler/                    # 链接健康检测的轻量级 HTTP 探测模块
│   │   │   ├── checker.js              # 并发请求调度与超时控制逻辑
│   │   │   └── reporter.js             # 检测结果聚合与状态变更通知
│   │   └── cache/                      # 内存缓存层，用于热点分类与统计数据的短期驻留
│   ├── frontend/                       # 前端用户界面源代码
│   │   ├── themes/                     # 内置主题模板文件与静态资源
│   │   │   ├── default/                # 默认列表式主题布局
│   │   │   └── card/                   # 卡片式主题布局
│   │   ├── assets/                     # 全局 CSS、JavaScript 及图标字体库
│   │   └── components/                 # 可复用的 Vue 单文件组件
│   ├── admin/                          # 管理后台前端界面
│   │   ├── pages/                      # 后台各个管理页面的视图组件
│   │   └── stores/                     # 使用 Pinia 管理后台状态（登录态、分类树等）
│   └── utils/                          # 通用工具函数库
│       ├── validator.js                # URL 格式校验与域名黑名单过滤
│       └── slugify.js                  # 分类名称转 URL 友好格式的辅助函数
├── config/                             # 项目全局配置文件目录
│   ├── default.json                    # 默认配置项（端口、数据库路径、日志级别）
│   └── production.json                 # 生产环境覆盖配置（关闭调试、启用压缩）
├── tests/                              # 单元测试与集成测试用例
│   ├── unit/                           # 各模块独立单元测试
│   └── integration/                    # API 端点端到端测试及数据库事务回滚测试
├── scripts/                            # 运维与开发辅助脚本
│   ├── backup.js                       # 数据库手动备份命令行工具
│   └── seed.js                         # 填充演示分类与链接数据的种子脚本
├── docs/                               # 完整项目文档所在目录
├── package.json                        # npm 依赖声明与脚本命令定义
├── README.md                           # 项目总览与快速入门文档（本文件）
└── LICENSE                             # MIT 开源许可证文本
```

## 贡献指南

1. 在 GitHub 上复刻本仓库至个人账户，并将复刻后的仓库克隆至本地开发环境。创建新分支时，请遵循 `feature/描述性名称` 或 `fix/问题编号` 的命名惯例，以区分不同的开发目的。
2. 本地完成代码修改后，确保所有新增功能均包含相应的单元测试用例，且现有测试套件能够全部通过。对于涉及前端界面的变更，请在提交前执行 `npm run build` 验证构建过程无错误。
3. 提交代码时，请按照约定式提交规范编写提交信息，格式为 `<类型>: <简短描述>`，其中类型包括 `feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test` 及 `chore`，以便于自动生成变更日志。
4. 将本地分支推送至个人复刻仓库，并通过 GitHub 界面发起拉取请求，目标分支为上游仓库的 `main` 分支。拉取请求描述中请清晰说明变更动机、实现方式及已完成的测试验证步骤。
5. 项目维护者将在收到拉取请求后的 5 个工作日内进行代码审查，可能会提出修改建议或询问设计决策理由。请保持关注并配合完善，直至变更被合并或关闭。

## 常见问题

**问：PortalX 能否部署在带有反向代理的子路径下，例如 https://example.com/portal/ ？**

答：可以。您需要在 `config/production.json` 中设置 `basePath` 字段为 `/portal`，并确保反向代理（如 Nginx）正确传递 `X-Forwarded-Prefix` 请求头。前端路由将自动根据该配置调整静态资源引用路径与页面跳转逻辑。需要注意的是，API 端点的基础路径也会同步调整，若外部程序调用 API，请相应更新请求 URL。

**问：链接健康检查功能是否会因为频繁请求导致源站压力过大？**

答：健康检查模块默认采用并发数受控的队列调度机制，最大并发请求数为 5，且每个链接两次检查之间的最小间隔为 3600 秒（1 小时）。对于同一域名下的多个链接，系统会自动进行请求合并，仅对该域名执行一次 TCP 连接探测，后续复用连接状态，从而有效避免对源站造成额外负担。您也可以在配置文件中调整 `checker.concurrency` 与 `checker.interval` 参数以适配自身需求。

**问：如果我希望将所有链接数据从 SQLite 迁移到 PostgreSQL，是否可行？**

答：项目核心数据访问层已采用 Knex 查询构建器编写，理论上支持切换至 PostgreSQL、MySQL 等主流关系型数据库。您需要手动安装对应的数据库驱动包，修改 `config/default.json` 中的 `database.client` 与 `database.connection` 配置项，并重新运行迁移脚本以初始化表结构。由于不同数据库的 SQL 方言存在细微差异，部分复杂的统计查询可能需要额外适配，建议在测试环境中充分验证后再应用于生产。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:35
