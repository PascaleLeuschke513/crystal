# BifenHub

BifenHub 是一个面向数据聚合与实时信息分发的开源技术资源导航站，定位于为开发者、数据分析师及运维工程师提供高可用的外链汇聚与健康检查能力。该项目不对上游数据做任何修改或二次加工，仅作为结构化索引层，帮助用户在复杂网络环境中快速定位权威比分、状态查询与历史数据接口。BifenHub 适用于需要频繁访问外部动态数据源但又希望降低接口管理成本的团队，亦可作为微服务架构中外部依赖治理的参考实现。

## 功能概览

- **多源外链统一纳管** 提供标准化的链接登记与分类机制，支持按域名、协议、响应时间等维度对上游资源进行标记与筛选。

- **主动式健康探测** 内置基于 HTTP/HTTPS 状态码与响应体关键字的探测任务，可周期性地检查每个注册链接的可达性与数据完整性。

- **结构化元数据输出** 将所有资源链接及其关联属性（如所属类别、探测间隔、超时阈值）以 JSON 或 YAML 格式导出，便于下游监控系统集成。

- **分组视图与标签系统** 支持对链接进行多级标签定义，例如按数据类型（实时比分、历史统计、队伍资料）或按数据源地区进行分类，并支持多标签组合筛选。

- **探测历史追溯** 记录每一次健康检查的响应码、耗时与异常摘要，提供按时间范围的查询接口，帮助定位上游服务的稳定性趋势。

- **配置热加载** 新增、修改或删除链接条目后，无需重启服务即可使探测配置生效，适合频繁变动的数据源管理场景。

- **轻量化部署** 无外部数据库依赖，所有状态数据可持久化至本地文件系统，亦可挂载至 Redis 作为共享缓存，适配容器化环境。

## 应用场景

- **实时赛事数据聚合** 开发体育数据面板时，需同时从多个比分服务商获取比赛进程。BifenHub 可将所有比分接口注册为外链，由探测模块持续验证可用性，前端仅需轮询 BifenHub 输出的健康链接列表，避免因单一数据源故障导致展示中断。

- **外部 API 网关前置检查** 在微服务网关层之前部署 BifenHub，作为外部依赖的“熔断预检”组件。当探测连续失败达到阈值时，可自动标记该链接为不健康，并触发告警，帮助运维团队快速隔离问题上游。

- **数据源迁移与灰度验证** 当计划从旧版数据接口切换至新版时，可同时注册新旧两组链接，通过标签区分环境。利用分组视图对比两者的健康率与平均响应时间，为切换决策提供量化依据。

- **多区域部署下的链路择优** 在跨地域的分布式系统中，不同区域访问同一外部服务的延迟差异较大。BifenHub 支持按探测源区域标记链接，并可输出各区域推荐的最优端点，辅助调度层进行智能路由。

## 快速开始

以下步骤假设您已安装 Go 1.20+ 或 Docker 环境。克隆仓库后，使用 Make 工具完成编译与启动。

```bash
# 克隆项目仓库
git clone https://github.com/bifenhub/bifenhub.git
cd bifenhub

# 安装项目依赖（使用 Go Modules）
go mod download

# 编译二进制文件
make build

# 以默认配置运行服务（监听 8080 端口，探测间隔 60 秒）
./bin/bifenhub --config ./configs/default.yaml
```

若使用 Docker，可直接执行：

```bash
docker build -t bifenhub:latest .
docker run -d -p 8080:8080 -v $(pwd)/data:/app/data bifenhub:latest
```

启动后，访问 `http://localhost:8080/api/v1/links` 可获取当前纳管的全部链接及其健康状态。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Go 工具链 | 1.20 或更高 | 编译与运行主程序，需支持 go:embed 特性 |
| Make | 3.81 或更高 | 用于执行构建脚本、测试与打包任务 |
| Git | 2.25 或更高 | 克隆仓库及版本管理，CI/CD 流程中必需 |
| Docker（可选） | 20.10 或更高 | 容器化部署时使用，非强制依赖 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 跨平台支持，生产环境推荐 Linux 内核 5.4+ |
| 网络 | 出站 80/443 端口开放 | 用于对外部链接执行 HTTP/HTTPS 探测 |
| 存储 | 至少 100 MB 可用空间 | 用于存放配置、日志及本地持久化状态文件 |

## 文档导航

| 层面 | 目录/文档 | 回答的问题 |
|------|----------|----------|
| 用户手册 | `docs/user-guide/quickstart.md` | 如何快速上手部署并接入第一个外部链接？ |
| 配置参考 | `docs/configuration/probe-settings.md` | 探测频率、超时、重试策略及告警阈值如何精细化调整？ |
| 运维指南 | `docs/operations/monitoring-integration.md` | 如何将 BifenHub 的健康数据接入 Prometheus 或 Grafana？ |
| 开发设计 | `docs/design/architecture-overview.md` | 内部模块划分、状态机设计及扩展点在哪里？ |

## 资源列表

本项目的核心价值在于对以下外部资源的聚合与健康管理。所有链接均保持原始格式原样收录，不进行任何协议转换或域名改写。

数据源类别 - 实时比分接口

<code>https://bifenwangg.org.cn</code>

<code>https://bifenwangh.org.cn</code>

<code>https://lanqiubifend.org.cn</code>

<code>https://lanqiubifene.org.cn</code>

<code>https://lanqiubifenf.org.cn</code>

<code>https://lanqiubifeng.org.cn</code>

<code>https://lanqiubifenh.org.cn</code>

## 项目结构

项目采用标准的 Go 工程布局，核心模块清晰分层，便于二次开发与维护。

```
bifenhub/
├── cmd/                          # 主程序入口
│   └── bifenhub/                 # 服务启动入口 (main.go)
│       └── main.go               # 解析命令行参数，初始化系统
├── internal/                     # 内部私有包，不对外暴露
│   ├── probe/                    # 探测引擎核心
│   │   ├── http_checker.go       # HTTP/HTTPS 健康检查实现
│   │   └── scheduler.go          # 周期性任务调度器
│   ├── registry/                 # 链接注册与存储
│   │   ├── store.go              # 内存存储及文件持久化接口
│   │   └── validator.go          # 链接格式与合法性校验
│   └── api/                      # RESTful API 服务层
│       ├── handler.go            # 路由与请求处理
│       └── response.go           # 统一响应封装
├── pkg/                          # 可被外部导入的公共库
│   └── health/                   # 健康状态枚举与工具函数
│       └── status.go             # 定义 Up/Down/Unknown 状态及转换逻辑
├── configs/                      # 配置文件模板
│   ├── default.yaml              # 默认配置（探测间隔、超时、日志级别）
│   └── example.links.yaml        # 示例链接注册列表
├── docs/                         # 完整文档目录
│   ├── user-guide/               # 用户指南
│   ├── configuration/            # 配置详解
│   ├── operations/               # 运维与监控
│   └── design/                   # 架构设计文档
├── scripts/                      # 辅助脚本
│   ├── bootstrap.sh              # 快速环境初始化
│   └── healthcheck.sh            # 外部独立健康检查脚本
├── test/                         # 集成测试与模拟数据
│   ├── mock/                     # Mock 上游服务模拟器
│   └── e2e/                      # 端到端测试用例
├── Makefile                      # 构建、测试、打包统一入口
├── go.mod                        # Go 模块依赖定义
└── README.md                     # 项目简介与快速导航（本文件）
```

## 贡献指南

我们欢迎社区提交 Bug 修复、功能增强及文档改进。请遵循以下流程以确保代码质量与协作效率。

1.  **提交 Issue 讨论** 在新建 Pull Request 之前，请先于 Issues 列表中查找是否已有相关讨论。若无，则创建一个新 Issue，清晰描述您发现的问题或希望新增的特性，并等待维护者反馈。

2.  **派生仓库并创建特性分支** 将本项目派生至您的个人账户下，然后基于 `main` 分支新建一个描述性的分支名称，例如 `feature/add-retry-policy` 或 `fix/probe-timeout`。

3.  **编写代码与测试** 遵循项目现有的代码风格（使用 `gofmt` 格式化）。对于新增功能，请补充对应的单元测试或集成测试，确保测试覆盖率达到 80% 以上。运行 `make test` 以验证所有测试通过。

4.  **更新文档与示例** 若您的变更涉及配置项或 API 行为变化，请同步更新 `docs/` 下的相关文档以及 `configs/` 中的示例配置文件。修改 README 中的功能列表（如适用）。

5.  **发起 Pull Request** 将您的分支推送到派生仓库，并向本仓库的 `main` 分支发起 Pull Request。描述中请关联对应的 Issue 编号，并列出本次变更的要点及自测结果。维护者将在 3 个工作日内完成审核。

## 常见问题

**Q: 探测模块是否会对上游服务造成过大压力？**

A: BifenHub 默认采用指数退避重试策略，且每个链接的探测间隔在配置中独立可调（最小 10 秒）。并发探测数量由 `probe.worker_pool_size` 参数控制，默认值为 5，避免瞬时高并发请求。建议根据上游服务的承载能力合理设置间隔，生产环境中可配合 `rate_limiter` 进行限流。

**Q: 如果某个外链长时间不可用，系统如何标记并恢复？**

A: 系统维护每个链接的连续失败计数器。当连续失败次数达到 `failure_threshold`（默认 3）时，该链接状态标记为 `DOWN`，API 返回的列表中不会包含该链接（除非显式请求 `include_unhealthy=true`）。一旦后续探测成功，计数器重置，状态立即恢复为 `UP`，无需人工介入。

**Q: 如何迁移 BifenHub 的配置与状态数据到另一台机器？**

A: 所有配置位于 `configs/` 目录，持久化状态（如探测历史统计、当前健康状态快照）默认存储在 `data/` 目录下的 JSON 文件中。您只需打包这两个目录，传输至新机器相同路径下，并以相同的命令行参数启动即可。若使用 Docker，需确保挂载卷包含上述目录。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:29
