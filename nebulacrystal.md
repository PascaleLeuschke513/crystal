# ResourceLink Hub

ResourceLink Hub 是一个面向开发者、技术研究人员与互联网内容分析从业者的高质量外链资源聚合与导航系统。本项目定位于解决互联网资源分散、失效链接难以追踪、跨领域技术资料检索效率低下等问题，通过人工筛选与自动化校验相结合的方式，维护一张结构清晰、持续更新的外链资源地图。目标用户包括开源社区贡献者、网络安全分析人员、数字内容研究机构以及需要系统化管理外部技术参考链接的工程团队。

系统提供链接分类存储、状态监控、标签检索与访问热度统计等核心能力，并以轻量级静态站点形态交付，可独立部署于内网或公网服务器。项目自身不存储任何第三方内容，仅作为索引层存在，严格遵守互联网资源引用规范。

## 功能概览

- **多维度分类导航**：按内容领域、资源类型、数据格式等维度对收录链接进行标签化分类，支持快速筛选与定位。

- **链接可用性自动检测**：内置定时任务，对已收录链接进行 HTTP 状态码检查，自动标记失效或重定向资源，生成异常报告。

- **全文标签检索**：基于倒排索引实现轻量级搜索功能，支持按资源名称、描述关键词、分类标签进行模糊匹配查询。

- **访问统计与热度排序**：记录每个外链资源的点击次数与最近访问时间，支持按热度、更新时间、添加时间等多种排序方式展示。

- **资源变更追踪**：对链接指向的页面内容进行哈希校验，当目标页面发生显著变化时记录变更日志，便于追踪外部信息的演进。

- **结构化数据导出**：支持将资源库导出为 JSON、CSV 或 YAML 格式，便于与其他数据分析工具或文档生成流水线集成。

- **用户自定义收藏集**：允许注册用户创建个人收藏夹，将常用资源分组管理，并支持收藏集的公开分享与协作编辑。

- **RSS 订阅源生成**：为每个分类标签和收藏集生成独立 RSS 订阅链接，方便用户通过阅读器跟进新增资源。

## 应用场景

- **技术文档编写与维护**：技术撰稿人在编写系统设计文档、API 参考或开发指南时，可使用本项目快速检索并引用权威的外部规范、标准或官方参考实现，确保引用的准确性和可追溯性。

- **网络安全威胁情报收集**：安全分析人员可将已知的威胁情报源、漏洞数据库、安全公告页面收录至系统中，利用自动检测功能监控这些关键资源的可达性变化，及时发现情报源失效或被篡改的情况。

- **学术研究与文献综述**：高校师生或科研机构在进行互联网现象研究、传播分析或数据挖掘时，通过本系统批量管理大量样本网站或数据接口地址，配合导出功能将链接列表导入爬虫框架或分析流水线。

- **开源项目文档站外链管理**：开源项目维护者可将项目 README、官网、Wiki 中引用的所有外部链接统一托管至 ResourceLink Hub，避免散落在各处导致链接不一致或失效后难以批量更新。

- **个人知识库外链索引**：知识管理爱好者可将博客、笔记、书签中积累的大量参考链接进行系统化整理，借助标签和搜索功能快速回溯，降低信息遗忘带来的重复检索成本。

## 快速开始

以下步骤指导您在本机部署并启动 ResourceLink Hub 服务。

```bash
# 步骤 1: 克隆代码仓库
git clone https://github.com/resourcelink-hub/resourcelink-hub.git
cd resourcelink-hub

# 步骤 2: 安装项目依赖（使用 pip 和 npm）
pip install -r requirements.txt
npm install --prefix frontend

# 步骤 3: 执行数据库初始化并启动服务
python scripts/init_db.py
python scripts/import_sample_links.py
python app.py --host 0.0.0.0 --port 8080
```

服务启动后，访问 `http://localhost:8080` 即可进入导航首页。默认管理员账号为 `admin`，初始密码在启动日志中打印，首次登录后请立即修改。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 及以上 | 后端服务运行环境，使用 Flask 框架 |
| Node.js | 18.x LTS 及以上 | 前端静态资源构建工具链依赖 |
| SQLite | 3.35 及以上 | 默认内置数据库，用于存储链接元数据与用户信息 |
| Redis | 7.0 及以上（可选） | 用于缓存访问统计和分布式任务锁，生产环境建议配置 |
| Nginx | 1.24 及以上（可选） | 推荐用于反向代理和静态资源缓存加速 |
| Git | 2.30 及以上 | 用于版本管理和通过 Git 钩子触发链接校验任务 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | `/docs/user-guide/` | 如何注册账号、添加链接、创建收藏集、使用搜索和分类浏览？ |
| 管理员指南 | `/docs/admin-guide/` | 如何进行链接审核、配置检测任务、管理用户权限和查看系统日志？ |
| 开发文档 | `/docs/developer-guide/` | 如何扩展新的链接解析器、自定义标签规则、贡献代码或编写插件？ |
| API 参考 | `/docs/api-reference/` | 所有 RESTful 接口的请求参数、响应格式、认证方式及错误码说明 |
| 部署运维 | `/docs/deployment/` | 如何在生产环境使用 Docker Compose 或 Kubernetes 集群部署？ |
| 设计说明 | `/docs/design/` | 系统架构图、数据模型 ER 图、任务调度机制和缓存策略设计 |

## 资源列表

以下为项目收录的核心外部资源链接，按内容类别分组展示。所有链接均经过初始校验，后续可用性由自动化检测任务持续跟踪。

网络直播内容类：

<code>https://wanghongfulizhibow.org.cn</code>

<code>https://guochanwanghongzhibozhuzaixianw.org.cn</code>

<code>https://guochanwanghongshipinzhibow.org.cn</code>

<code>https://wanghongzhibomianfeiguankanw.org.cn</code>

<code>https://meinvzhibozaixiankanw.org.cn</code>

<code>https://guochanwanghongfulishipinw.org.cn</code>

<code>https://rihanzhibofulishipinw.org.cn</code>

## 项目结构

```
resourcelink-hub/
├── app/                                # 后端应用主目录
│   ├── __init__.py                     # Flask 应用工厂函数，注册蓝图与扩展
│   ├── routes/                         # 路由层，按功能域拆分
│   │   ├── auth.py                     # 用户认证相关接口（登录、注册、令牌刷新）
│   │   ├── links.py                    # 链接增删改查、标签管理、导入导出接口
│   │   ├── collections.py              # 收藏集创建、修改、分享及成员管理
│   │   └── stats.py                    # 访问统计、热度数据查询接口
│   ├── models/                         # 数据模型层，使用 SQLAlchemy ORM
│   │   ├── user.py                     # 用户表模型与密码哈希方法
│   │   ├── link.py                     # 链接主表、标签关联表、访问日志表模型
│   │   └── task.py                     # 定时检测任务配置与执行记录模型
│   ├── services/                       # 业务逻辑服务层
│   │   ├── checker.py                  # 链接可用性检测服务，含超时重试与代理支持
│   │   ├── crawler.py                  # 轻量级页面内容抓取与标题、描述提取
│   │   ├── search.py                   # 基于 SQLite FTS5 的全文检索引擎封装
│   │   └── rss.py                      # RSS 订阅源生成服务
│   └── utils/                          # 通用工具函数
│       ├── validators.py               # URL 规范化、域名黑名单校验
│       └── cache.py                    # Redis 缓存操作封装
├── frontend/                           # 前端静态资源目录
│   ├── src/                            # Vue 3 单页应用源代码
│   │   ├── components/                 # 可复用 UI 组件（导航卡、搜索框、列表分页）
│   │   ├── views/                      # 路由页面（首页、分类浏览、收藏夹、管理后台）
│   │   └── store/                      # Pinia 状态管理（用户状态、筛选条件缓存）
│   └── dist/                           # 构建产物目录（由 npm run build 生成）
├── scripts/                            # 运维与工具脚本
│   ├── init_db.py                      # 创建数据库表结构与默认管理员账户
│   ├── import_sample_links.py          # 导入资源列表中的初始链接数据
│   └── run_checker.py                  # 手动触发全量链接检测的独立脚本
├── tests/                              # 单元测试与集成测试
│   ├── test_models.py                  # ORM 模型增删改查及约束测试
│   └── test_checker.py                 # 链接检测服务的模拟网络请求测试
├── config/                             # 配置文件目录
│   ├── development.py                  # 开发环境配置（开启调试、使用本地 SQLite）
│   └── production.py                   # 生产环境配置（使用 PostgreSQL、Redis 地址）
├── docker-compose.yml                  # Docker Compose 编排文件（含应用、Redis、Nginx）
├── requirements.txt                    # Python 后端依赖列表（Flask、SQLAlchemy、Celery 等）
└── README.md                           # 项目主说明文档（即本文档）
```

## 贡献指南

1. 查阅问题列表与路线图：访问 GitHub Issues 页面查看已标记的 `good-first-issue` 和 `help-wanted` 标签任务，或于 Discussions 中提出新功能建议并等待社区反馈。

2. 派生仓库并创建功能分支：从主仓库派生副本至个人账户，然后克隆本地并新建分支，分支命名建议采用 `feature/功能简述` 或 `fix/问题简述` 格式。

3. 编写代码并执行本地测试：确保新增或修改的代码通过现有单元测试，并为新增功能补充对应的测试用例。运行 `pytest tests/` 验证所有测试通过，同时使用 `flake8` 和 `prettier` 保持代码风格一致。

4. 签署开发者原创声明：在拉取请求描述中确认代码为本人原创或已获得合规授权，同意将代码以 MIT 许可证贡献至本项目。

5. 提交拉取请求并参与评审：将分支推送至派生仓库后向主仓库的 `main` 分支发起 Pull Request，填写变更摘要、关联 issue 编号和测试结果摘要。评审过程中根据 reviewer 意见进行修改，直至合并。

## 常见问题

**问：链接检测服务为什么偶尔会报告假阳性失效？**

答：部分目标站点可能会对自动化请求进行拦截或限制访问频率，也可能临时返回 5xx 状态码。系统默认使用三次重试策略（间隔 5 秒、10 秒、30 秒）并配置了常见 User-Agent 轮换，但仍无法完全规避服务端限流。建议管理员在配置中调整检测超时时间或设置代理池，对于持续误报的链接可手动标记为白名单。

**问：是否可以导入浏览器书签或第三方收藏夹数据？**

答：当前版本支持导入 Netscape 格式的 HTML 书签导出文件（Chrome、Firefox 均支持导出此格式），以及 CSV 格式的简单链接列表。管理员可在后台管理界面的"批量导入"功能中选择文件上传，系统会自动解析并去重。对于其他格式，可参考开发者文档编写自定义解析适配器。

**问：多用户同时编辑同一收藏集时如何保证数据一致性？**

答：系统采用乐观锁机制，在收藏集版本号上实现冲突检测。当两个用户同时修改时，后提交的变更会收到版本冲突提示，并展示当前最新内容，用户可选择合并或覆盖。对于高并发场景，建议管理员在收藏集设置中启用"仅管理员可编辑"模式。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:29
