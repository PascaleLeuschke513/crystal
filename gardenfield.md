# OpenLink Catalog

OpenLink Catalog 是一个面向开发者、技术内容策展人与数据研究机构的外链资源聚合与规范化管理平台。该项目定位于解决分散在网络各处的优质技术资源、数据源与参考链接难以系统化整理、可访问性不稳定、以及缺乏统一元数据描述的问题。OpenLink Catalog 本身不存储任何第三方内容，仅提供结构化的链接索引、可用性探测与基础分类标签体系，帮助用户快速定位特定领域的高价值外链资源，适用于技术文档编写、数据采集任务配置、以及自动化测试环境中的外部依赖管理。

目标用户包括需要批量管理外部 URL 的运维工程师、维护技术导航站的内容创作者、进行市场信息采集的数据分析师，以及希望建立可维护外链资产库的技术团队。项目通过标准化的链接状态标记与简单的分类视图，降低外链资源的管理成本与失效风险。

## 功能概览

- **多格式外链导入** 支持单条添加、批量文本导入以及从现有书签文件解析链接，自动去重并识别基础协议信息。

- **实时可用性检测** 定时对库内链接进行 HTTP 状态检查，标记异常链接并生成可用性趋势报表，支持手动触发重新检测。

- **分类标签与全文检索** 用户可为每条链接自定义分类标签与简短备注，后续可通过标签筛选或备注关键词快速定位目标资源。

- **公开只读视图生成** 为每个分类生成独立的公开访问页面，方便将整理好的外链集以干净列表形式分享给协作者或下游用户。

- **链接元数据自动补全** 根据域名与路径特征，自动尝试获取页面标题、描述与关键词信息，辅助用户完善资源描述。

- **访问统计与热度排序** 记录每条链接在系统内的点击次数，支持按热度排序，帮助识别高频使用的核心资源。

- **数据导入导出** 支持将整个链接库导出为 CSV、JSON 或纯文本列表格式，也支持从相同格式文件恢复数据，便于迁移与备份。

## 应用场景

- **技术文档外链管理** 技术写作团队可使用 OpenLink Catalog 集中存放所有参考文档、API 规范与示例代码的原始链接，在撰写文档时直接引用系统生成的稳定链接标识符，避免因原始 URL 变更导致文档失效。

- **数据采集任务配置** 数据采集工程师可将各类数据源接口、公共数据集页面与备用镜像链接录入系统，通过可用性检测功能定期排查失效源，确保采集任务的稳定性。

- **技术导航站内容编排** 独立开发者或社区维护者可使用本系统作为后台数据源，将整理后的外链按分类生成静态页面，快速搭建轻量级技术导航网站，无需重复编写链接管理逻辑。

- **项目外部依赖审计** 企业合规团队可导入所有第三方服务、SDK 下载页与授权文档链接，借助系统记录的访问日志与可用性历史，定期审计外部依赖的合规状态与可访问性。

- **个人知识库资源索引** 研究人员可将阅读过程中积累的论文链接、数据集页面与工具站统一录入，配合标签与备注功能构建可检索的外链知识索引，减少重复查找时间。

## 快速开始

以下步骤指导您在本地环境快速启动 OpenLink Catalog 服务。

```bash
# 克隆项目仓库
git clone https://github.com/openlink-catalog/openlink-catalog.git

# 进入项目目录
cd openlink-catalog

# 安装依赖（使用 pip 与 requirements.txt）
pip install -r requirements.txt

# 初始化本地数据库与配置
python manage.py init --env development

# 启动开发服务器
python manage.py runserver --port 8080
```

执行完毕后，访问控制台输出中显示的本地地址即可开始使用。首次启动会自动创建默认管理员账户，初始密码将在终端输出中提示，请及时修改。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 及以上 | 核心运行时，用于后端服务与任务调度 |
| pip | 22.0 及以上 | Python 包管理工具，用于安装依赖库 |
| SQLite | 3.35 及以上 | 默认内嵌数据库，用于存储链接元数据与配置 |
| Git | 2.30 及以上 | 用于克隆仓库及版本管理 |
| curl | 7.68 及以上 | 用于可用性检测模块的外部请求探测 |
| tzdata | 最新稳定版 | 时区数据处理，影响任务调度与日志时间戳 |
| PyYAML | 6.0 及以上 | 用于解析自定义配置文件格式 |
| markdown | 3.4 及以上 | 用于生成只读视图中的说明文本渲染 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 入门指南 | /docs/quickstart.md | 如何在 5 分钟内完成首次配置并录入第一个链接？ |
| 操作手册 | /docs/user-guide.md | 如何进行批量导入、可用性检测与分类管理？ |
| 部署参考 | /docs/deployment.md | 生产环境如何配置反向代理、进程守护与 HTTPS？ |
| 开发文档 | /docs/development.md | 如何二次开发自定义检测插件或扩展 API 接口？ |
| 配置说明 | /docs/configuration.md | 所有环境变量与配置文件的详细字段含义是什么？ |
| 常见问题 | /docs/faq.md | 遇到异常状态码、检测超时或数据迁移错误如何处理？ |

## 资源列表

### 直播与视频内容资源

<code>https://wanghongzhibofulizaixian.org.cn</code>

<code>https://nvzhubozshipinzaixianguankan.org.cn</code>

<code>https://xingganmeinvzhibotiaowu.org.cn</code>

<code>https://hanguomeinvzhuborewu.org.cn</code>

<code>https://zaixianbofangzhubo.org.cn</code>

<code>https://zhubozhibozaixianguankan.org.cn</code>

<code>https://wanghongzhibozaixianshipinw.org.cn</code>

## 项目结构

```
openlink-catalog/
├── app/                                # 主应用模块
│   ├── api/                            # RESTful API 路由与视图
│   │   ├── v1/                         # API 版本 v1 实现
│   │   │   ├── endpoints/              # 各资源端点（链接、分类、检测）
│   │   │   └── schemas/                # 请求与响应数据模型定义
│   ├── core/                           # 核心业务逻辑
│   │   ├── checker.py                  # 可用性检测引擎实现
│   │   ├── parser.py                   # 链接解析与元数据提取
│   │   └── scheduler.py                # 定时任务调度配置
│   ├── models/                         # 数据模型与 ORM 映射
│   │   ├── link.py                     # 链接实体模型
│   │   ├── tag.py                      # 标签模型
│   │   └── audit.py                    # 访问与检测日志模型
│   ├── static/                         # 静态资源（CSS、JS、图片）
│   │   ├── css/                        # 基础样式与主题
│   │   └── js/                         # 前端交互脚本
│   └── templates/                      # 公开只读视图与管理界面模板
│       ├── public/                     # 公开分类视图页面
│       └── admin/                      # 后台管理操作界面
├── config/                             # 配置文件目录
│   ├── default.yaml                    # 默认配置（端口、检测间隔、日志级别）
│   ├── development.yaml                # 开发环境覆盖配置
│   └── production.yaml.example         # 生产环境配置示例
├── data/                               # 本地数据存储目录
│   ├── db/                             # SQLite 数据库文件位置
│   └── backups/                        # 数据导出备份存放位置
├── docs/                               # 完整文档目录（见文档导航）
├── scripts/                            # 辅助运维脚本
│   ├── import_batch.py                 # 批量导入外部链接列表脚本
│   ├── export_csv.py                   # 导出链接库为 CSV 格式脚本
│   └── health_check.py                 # 独立健康检查工具
├── tests/                              # 单元测试与集成测试
│   ├── unit/                           # 各模块独立单元测试
│   └── integration/                    # 端到端 API 测试
├── requirements.txt                    # 生产环境 Python 依赖列表
├── requirements-dev.txt                # 开发与测试额外依赖
├── manage.py                           # 项目统一管理命令行入口
├── README.md                           # 项目概述与快速入门（当前文件）
└── LICENSE                             # MIT 许可证文本
```

## 贡献指南

1. 查阅 issues 列表，选择未被认领且标记为 "help wanted" 或 "good first issue" 的任务，在 issue 下回复表明认领意向，等待维护者确认。

2. 从主仓库 fork 项目到个人账户，克隆 fork 后的仓库到本地，并按照快速开始步骤搭建开发环境。建议在独立分支上进行修改，分支命名遵循 `feature/功能简述` 或 `fix/问题简述` 格式。

3. 完成代码修改后，确保所有现有单元测试通过，并为新增功能或修复编写对应的测试用例。运行 `python manage.py test` 执行完整测试套件。提交前执行 `scripts/health_check.py` 确保基础检测功能未受影响。

4. 编写清晰的提交信息，遵循常规提交格式，例如 `feat: 添加链接导入去重逻辑` 或 `fix: 修复检测超时配置未生效问题`。提交后推送分支到个人远程仓库，并发起 Pull Request 到主仓库的 main 分支。

5. Pull Request 描述中需写明解决的问题编号、修改内容概要以及测试覆盖情况。维护者会在 3 个工作日内进行审核，必要时会提出修改意见。审核通过后由维护者合并。

## 常见问题

**问：可用性检测返回 403 或 429 状态码，是否表示链接失效？**

答：不一定。403 可能表示目标服务器拒绝自动化请求，429 表示请求频率过高被限流。系统默认将 4xx 和 5xx 状态码标记为异常，但用户可在配置中自定义状态码白名单。若确认链接在浏览器中可正常访问，可在管理界面手动将链接状态标记为有效，并调整检测请求头模拟浏览器行为。

**问：如何迁移数据到另一台服务器？**

答：首先在当前服务器执行导出命令 `python manage.py export --format json --output data/backups/export.json`，将导出文件复制到目标服务器。在目标服务器完成安装配置后，执行导入命令 `python manage.py import --file data/backups/export.json`，系统会自动重建数据库记录。若使用 SQLite，也可直接复制数据库文件，但需注意目标环境运行用户需具备读写权限。

**问：系统支持 HTTPS 访问吗？**

答：开发环境默认使用 HTTP。生产环境部署时，推荐在前端使用 Nginx 或 Caddy 作为反向代理并配置 TLS 证书，应用本身无需修改即可通过代理正常处理请求。具体反向代理配置示例请参考 `docs/deployment.md` 中的相关章节。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:08:29
