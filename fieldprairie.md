# Hydra Link

Hydra Link 是一个面向开发者、技术研究员及互联网内容分析从业者的高可用外链资源导航与聚合平台。该项目旨在解决技术信息分散、优质资源发现成本高的问题，通过人工审核与自动化健康检查相结合的方式，为用户提供结构化、高可靠性的网络资源索引服务。本项目定位为技术工具，不涉及对资源内容的任何主张，仅提供公开网络地址的整理与分类导航。

## 功能概览

- **智能分类索引**：资源按内容主题、域名类型、应用场景进行多维度标签化分类，支持快速筛选与定位。
- **全链路健康监控**：内置定时巡检模块，对收录的每个URL进行可达性、响应状态码及SSL证书有效期检测，状态变更实时记录。
- **自定义资源集**：支持用户创建个人收藏集，将常用外部链接分组管理，并支持导入/导出为JSON格式。
- **快速模糊搜索**：基于域名关键词、分类标签或描述信息进行实时搜索，并展示资源状态标识（正常/异常/待验证）。
- **对外API接口**：提供RESTful API，允许第三方程序以编程方式查询资源列表、状态及分类树，便于集成到其他运维或分析系统。
- **静态化部署支持**：核心导航数据可生成为静态JSON文件，支持无后端部署在对象存储或CDN上，确保高并发下的访问速度。
- **访问日志分析**：提供轻量级访问统计仪表板，展示热门资源点击排行与时段分布，辅助用户发现高价值链路。

## 应用场景

1.  **技术研究素材收集**：研究人员可分类收藏特定领域的公开站点，例如流媒体技术分析、网络内容生态观察等，通过健康监控功能确保研究链路的持续可用。
2.  **运维巡检辅助**：运维工程师可将Hydra Link作为外部依赖资源的统一配置中心，通过其API获取最新资源地址列表，并集成到自动化部署或监控告警流程中。
3.  **内容合规性参考**：内容审核或风控团队可利用本平台的分类索引，快速对比和识别不同来源的公开信息，作为风险研判的辅助数据源。
4.  **个人知识库构建**：开发者或极客用户可使用自定义资源集功能，将分散在多个文档中的工具链接、技术博客、API文档入口统一管理，形成私有的导航页。

## 快速开始

以下步骤将帮助您在本地环境快速启动Hydra Link实例。

```bash
# 1. 克隆项目仓库
git clone https://github.com/hydra-link/hydra-link.git
cd hydra-link

# 2. 安装依赖（使用npm）
npm install

# 3. 启动开发服务器（默认端口3000）
npm run dev
```

启动成功后，访问 `http://localhost:3000` 即可查看本地导航界面。首次启动将自动加载示例资源数据集。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | 18.x LTS 或更高 | 运行时环境，用于执行核心服务与构建脚本 |
| npm | 9.x 或更高 | 包管理器，用于安装项目依赖 |
| SQLite3 | 系统内置或自动构建 | 轻量级数据库，用于存储用户配置和状态数据（生产环境可替换为PostgreSQL） |
| Git | 2.x 或更高 | 用于克隆仓库及版本管理 |
| 现代浏览器 | Chrome 90+, Firefox 88+, Edge 90+ | 用于访问管理界面，支持ES2020语法 |
| 网络连接 | 稳定出网 | 首次启动及资源状态检测时需要访问外网，内网部署需配置代理策略 |

## 文档导航

| 层面 | 目录/章节 | 回答的问题 |
| :--- | :--- | :--- |
| 用户指南 | /docs/user-guide | 如何添加资源、创建分类、设置健康监控规则及使用API接口？ |
| 部署手册 | /docs/deployment | 如何在生产环境（Docker、PM2、静态托管）部署Hydra Link？ |
| 开发文档 | /docs/development | 项目架构、核心模块说明、数据库Schema及如何提交代码？ |
| 配置参考 | /docs/configuration | 环境变量、配置文件字段详解及自定义主题样式方法？ |

## 资源列表

### 分类：流媒体内容索引

<code>https://qingqingcaoyuanzhongwenzimub.org.cn</code>

<code>https://wanghongzhibozaixianshipin.org.cn</code>

<code>https://wanghongfulizhibo.org.cn</code>

<code>https://guochanwanghongzhibozhuzaixian.org.cn</code>

<code>https://guochanwanghongshipinzhibo.org.cn</code>

<code>https://wanghongzhibomianfeiguankan.org.cn</code>

<code>https://meinvzhibozaixiankan.org.cn</code>

## 项目结构

```
hydra-link/
├── src/                        # 核心源代码目录
│   ├── api/                    # RESTful API 路由与控制器
│   │   ├── v1/                 # API v1版本实现
│   │   └── middleware/         # 鉴权、日志、限流中间件
│   ├── core/                   # 核心业务逻辑
│   │   ├── crawler/            # 资源健康状态检测模块（含重试与超时策略）
│   │   ├── classifier/         # 标签分类与索引引擎
│   │   └── notifier/           # 状态变更告警通知模块（支持Webhook）
│   ├── models/                 # 数据模型定义（Sequelize/TypeORM）
│   │   ├── resource.model.js   # 资源主表模型
│   │   └── tag.model.js        # 标签与关联表模型
│   ├── services/               # 服务层，封装外部接口调用
│   │   ├── cache.service.js    # Redis缓存服务封装
│   │   └── queue.service.js    # 任务队列（用于异步检测）
│   ├── ui/                     # 前端管理界面源码（React/Vue）
│   │   ├── components/         # 可复用UI组件
│   │   ├── pages/              # 页面视图
│   │   └── hooks/              # 自定义React Hooks
│   └── utils/                  # 通用工具函数（日志、验证、格式化）
├── config/                     # 配置文件目录
│   ├── default.json            # 默认配置（端口、数据库连接）
│   └── production.json         # 生产环境覆盖配置
├── data/                       # 本地数据存储（SQLite文件或初始化JSON）
│   └── seed/                   # 初始示例资源数据
├── tests/                      # 单元测试与集成测试用例
│   ├── unit/                   # 单元测试
│   └── integration/            # API及数据库集成测试
├── docs/                       # 详细文档源文件（Markdown）
├── scripts/                    # 运维脚本（启动、备份、迁移）
├── public/                     # 静态资源（favicon, robots.txt）
├── .env.example                # 环境变量模板
├── Dockerfile                  # 容器化构建文件
├── package.json                # 项目依赖与脚本定义
└── README.md                   # 项目主文档（本文件）
```

## 贡献指南

我们欢迎并感谢任何形式的贡献。请遵循以下步骤参与项目：

1.  **提交议题**：在提交Pull Request之前，请先在Issues区域创建议题，描述您要修复的问题或新增的功能，避免重复工作。
2.  **复刻仓库**：将主仓库复刻（Fork）至您的个人账户，并克隆到本地开发环境。
3.  **创建分支**：基于 `main` 分支创建一个新的功能分支，分支命名请遵循 `feature/xxx` 或 `fix/xxx` 格式。
4.  **编码与测试**：编写代码并确保新增或修改的部分通过所有单元测试。请为新增的核心逻辑添加相应的测试用例。
5.  **发起请求**：完成本地验证后，将分支推送至您的复刻仓库，并向主仓库的 `main` 分支发起Pull Request。请在请求描述中清晰引用关联的议题编号，并简述改动内容。

## 常见问题

**Q: 内置的健康检查会频繁访问目标站点，是否会对目标服务器造成压力？**

A: 健康检查模块默认采用渐进式间隔策略。对于状态正常的站点，检测间隔为24小时；对于状态异常或新添加的站点，检测间隔会缩短至1小时，并在连续3次失败后进入告警状态。检测请求使用标准的 `HEAD` 或 `GET` 方法，并设置合理的超时时间（5秒），且每次检测均携带 `User-Agent: HydraLink-HealthCheck/1.0` 标识，以便目标服务器识别。

**Q: 我可以将Hydra Link完全部署在无外网访问的内部网络中吗？**

A: 可以。项目在无外网环境下仍可完整运行，所有UI和管理功能均不受影响。您只需将初始化数据（`data/seed` 目录下的JSON文件）替换为内网地址列表，并关闭或调整健康检查模块的调度器（通过 `config/default.json` 中的 `scheduler.enabled` 配置项设为 `false`）。静态部署模式则完全不依赖外网。

**Q: 如何备份我的自定义资源集和分类数据？**

A: 数据持久化存储于SQLite数据库文件（位于 `data/hydra-link.db`）或您配置的外部数据库中。对于SQLite，直接备份该文件即可。您也可以通过UI界面的“导出数据”功能，将所有资源数据（不含健康日志）导出为JSON格式进行手动备份。建议结合操作系统的定时任务（如cron）定期执行备份脚本。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:07:51
